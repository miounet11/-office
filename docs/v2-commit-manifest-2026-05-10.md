# V2 Commit Manifest — 2026-05-10

> Coordinator-reversible files accumulated across the multi-session V2
> coordinator scope. **None of these are production C++**. All are
> documentation, schemas, fixtures, harness scripts, CI, or ledger
> append-only state.
>
> **User decision required**: approve commit (full set / per-group /
> reject). Coordinator will not commit without explicit authorization
> per `clavue.md`.

## Tracked-modified files (already in index, content changed)

| Path | Group | Why touched |
|---|---|---|
| `.agent/goals/2026-05-08-v2-ai-native/ledger.jsonl` | ledger | Append-only audit trail; +L1..L27 across V2 work (no rewrites) |
| `.github/workflows/build-installers.yml` | CI | I2 fix: add `--with-lang=zh-CN` (mac+win), `--with-macosx-bundle-identifier=com.kdoffice.app` (mac) |
| `README.md` | docs | V2 / V1.5 milestone notes |
| ~~`bin/bffvalidator.sh`~~ | — | **REVERTED 2026-05-10 turn 6**: working-tree had unauthorized mode-bit change `100644→100755` (clavue.md rule violation). `git checkout --` restored. NOT in commit. |
| `bin/intelligent-contract-fixtures.sh` | bin | I1 fix: extended-naming branch (`<schema>.<status-token>.json` → `expectation=valid` against `<schema>.schema.json`); strict-pair semantics preserved |
| `docs/product/v2-master-plan.md` | docs | V2 master plan body (G1 acceptance ≥3000 chars); count-bump field removed in L23 |

## Untracked-new files (need `git add` then commit)

### docs/ — V2 plans, narratives, handoffs

- `docs/CLAUDE-NOTES.md` — durable guidance not regenerated by `clavue /init`; V2 hard invariants section
- `docs/product/v2/day0-skeleton-landed.md` — Day-0 skeleton narrative
- `docs/product/v2/day1-progress.md` — Day-1{a..h} per-step rationale
- `docs/product/v2/lane-status.md` — living crosswalk (single living-count source of truth)
- `docs/v2-coordinator-handoff-2026-05-10.md` — session-to-session blocker/scope/gate snapshot
- `docs/v2-commit-manifest-2026-05-10.md` — **this file**

### docs/schemas/ — V2 schemas (locked, additionalProperties:false)

- `docs/schemas/provider-evidence.schema.json` — V2 per-call audit envelope (W1+W3); 17-token status enum
- `docs/schemas/apply-plan-runtime.schema.json` — V2 W3 per-call ApplyPlan envelope (D7 partial resolve); per-kind patch shape lands incrementally with each `SwUndoApplyPatch` impl

### docs/schemas/fixtures/ — V2 fixture roster (8 new)

- `provider-request.{ok,policy-denied,provider-error}.json` — extended-name (resolved by bin/ branch)
- `provider-evidence.{valid,invalid,apply-plan-failure}.json` — strict pair + extended
- `apply-plan-runtime.{valid,invalid}.json` — strict pair (3 patch kinds in valid; 5 distinct violations in invalid)

### tests/ — V2 contract harnesses

- `tests/v2-day0-skeleton-test.sh` — H1 doc structure
- `tests/v2-plan-baseline-test.sh` — H2 specs + 26 fixtures / 11 schemas
- `tests/v2-provider-evidence-schema-test.sh` — H3 schema↔C++ drift lock

### .github/workflows/ — CI

- `.github/workflows/v2-contract-harnesses.yml` — auto-runs H1-H4 on PR

## Files explicitly NOT in this commit

- `workdir/`, `instdir/`, `test-install/`, `autom4te.cache/`, `config*.{log,status}` — build artifacts (already tracked from prior commits per `project_build_artifacts_tracked.md`); never `git add .`
- `sysui/desktop/macosx/LaunchConstraint.plist` — pre-existing modification, unrelated to V2 scope
- `.agent/goals/2026-05-08-v1.5-completion/*` — V1.5 milestone files; require separate V1.5-scoped commit decision

## Verification — what is green right now

```
H1 v2-day0-skeleton:        passed (26 checks)
H2 v2-plan-baseline:        passed (21 checks, 26 fixtures / 11 schemas)
H3 v2-provider-evidence:    schema match, 3/3 fixtures
H4 V1.5+V2 fixtures:        26 passed / 0 failed
ai-native cppunit suite:    OK(77) — last verified pre-session
V1.5 27/27 strict roundtrip: untouched
```

## Suggested commit groupings (if user prefers split)

### Group A — V2 contract harness baseline (lowest risk)

Files: `.github/workflows/v2-contract-harnesses.yml`, `tests/v2-*.sh`,
`bin/intelligent-contract-fixtures.sh`, `docs/schemas/{provider-evidence,
apply-plan-runtime}.schema.json`, `docs/schemas/fixtures/*.json`,
`docs/product/v2/lane-status.md`, `docs/CLAUDE-NOTES.md`

Rationale: All under coordinator-reversible scope; CI proves correctness;
no production code touched. This is the production-ready harness suite.

### Group B — V2 narratives & handoff (low risk, doc-only)

Files: `docs/product/v2-master-plan.md`,
`docs/product/v2/day{0-skeleton-landed,1-progress}.md`,
`docs/v2-coordinator-handoff-2026-05-10.md`,
`docs/v2-commit-manifest-2026-05-10.md`, `README.md`,
`.agent/goals/2026-05-08-v2-ai-native/ledger.jsonl`

Rationale: Project-narrative state. Append-only ledger needs the matching
narrative committed atomically.

### Group C — CI installer drift (I2 partial fix)

Files: `.github/workflows/build-installers.yml`

Rationale: Lang + bundle-id drift fixes landed; downstream-branding D5
still gated. Commit message should call out D5 remains open.

## Blockers requiring user action (out of coordinator scope)

| ID | Decision |
|---|---|
| D1 | Authorize `sw/source/uibase/app/docsh*.cxx` (W3 Day-1b wiring) |
| D2 | Authorize `kqoffice/source/ai/provider/` (W1 Day-1a OllamaAdapter) |
| D3 | Authorize `cui/source/dialogs/commandpalette/` (W2 Day-1b popover) |
| D5 | Pick `downstream-branding/` source (commit / submodule / skip) |
| D6 | Re-scope worker owned-paths from non-existent `src/**` to `<module>/source/**` |
| Commit | Approve full / partial commit groups above |
