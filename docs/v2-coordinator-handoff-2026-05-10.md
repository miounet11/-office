# V2 Coordinator Handoff — last refreshed 2026-06-11 (L242)

> Authoritative wait-state snapshot. Read this first if a previous
> session ended in /goal mode without explicit user authorization.
>
> **Refresh policy**: when significant locks/baselines change, update
> the four numbered sections inline. Do not append historical
> snapshots — the lane-status.md ledger row count is canonical for
> "when". The session-id source links live in the per-row notes of
> `.agent/goals/2026-05-08-v2-ai-native/ledger.jsonl`.

## State at handoff

- All 10 V2 contract harnesses were green at the last full sweep; H9 is now 278 checks and H10 is now 23 checks. L146-L152 add local real-Ollama, app launch/init, document-processing, and UNO/suite dispatch smokes. L153-L160 build the visible current-bundle proof ladder through GUI readiness, exact pid attribution, File > New > Text Document, CommandPalette UNO/AX, physical Cmd+Shift+K, and Cowork Cmd+Shift+T dialog evidence. L162 restores the temporary L161 GUI/AX attribution regression. L163 proves visible Writer Select-to-act, L164 proves visible DiffReview Accept/Reject, L165 proves the visible Cowork task loop, L166 proves pending/running rows, L167 proves the live nonblocking pending→running transition, L168 proves macOS native notification submit/dispatch/stored-review-open in the visible Cowork smoke, L169 proves visible DiffReview Rewrite→Accept/Reject through a real `ollama: qwen3:0.6b` provider, L170 reduces `UITest_sw_select_to_act` to one known svp skip, L171 makes the Writer factory document-window suite zero-skip, L172 adds zero-skip Calc/Impress factory document-window UITest targets, L173 proves visible Calc/Impress Select-to-act action clicks on the exact current-builddir pid, L174 resolves the Writer Start Center creation-path svp hang, L175 refreshes H10 source archive classification for the expanded dirty tree, L176 emits explicit per-batch source archive path lists for safe commit planning, L177 activates the V3 H8 connector-manifest contract gate, L178 activates the V3 H9 eval-baseline seed, L179 activates the V3 H10 LocalCloud no-egress config contract, L180 activates the V3 H11 perf-baseline target contract, L181 activates the V3 H12 crash-recovery target contract, L182 activates the V3 W5 eval-report self-test, L183 activates the V3 W6 agent-step-plan self-test, L184 activates the V3 W4 audit-log-entry self-test, L185 activates the V3 W3 knowledge-index-chunk self-test, L186 activates the V3 W3 knowledge-index-query/result self-test, L187 activates the V3 W4 policy-tenant self-test, L188 activates the V3 W6 agent-step-result-state self-test, L189 activates the V3 W7 companion-contract self-test, L190 activates the V3 W8 sync-message self-test, L191 activates the V3 W9 onboarding-flow self-test, L192 activates the V3 W9 starter-pack self-test, L193 activates the V3 W9 edition-policy self-test, L194 activates the V3 W9 i18n-locale self-test, L195 activates the V3 W9 manual-docs self-test, L196 activates the V3 W9 distribution-update self-test, L197 activates the V3 W9 error-recovery-ux self-test, L198 activates the V3 W9 release-ga-checklist self-test, L199 activates the V3 W1 in-app-chat fixture self-test, L200 tightens the V3 W1 CommandPalette chat fallback entry-route design, L201 locks the V3 W1 explicit context syntax policy, L202 locks the V3 W1 Markdown rendering subset policy, L203 locks the V3 W1 per-document local history policy, L204 locks the V3 W1 streaming UI state policy, L205 locks the V3 W1 context autocomplete/no-conflict policy, L206 locks the V3 W2 connector manifest trust-chain policy, L207 locks the V3 W2 connector read-only/writeback policy, L208 locks the V3 W2 connector token-refresh policy, L209 locks the V3 W2 connector auth-flow policy, L210 locks the V3 W5 eval fixture schemas while raising H9 to 7 checks, L211 locks the V3 W5 capability eval reference baseline to V2 GA acceptance while raising H9 to 8 checks, L212 locks the V3 W5 LLM-judge reproducibility policy while raising H9 to 9 checks, L213 locks the V3 W5 per-release eval report archive policy while raising the W5 report self-test to 10 checks, L214 locks the V3 W6 forward-only DAG dependency policy while raising the W6 agent-step-plan self-test to 8 checks, L215 locks the V3 W6 fail-closed plan validation policy while raising the W6 agent-step-plan self-test to 9 checks, L216 locks the V3 W6 approval UX policy while raising the W6 agent-step-plan self-test to 10 checks, L217 locks the V3 W6 cross-session resume policy while raising the W6 agent-step-plan self-test to 11 checks, L218 locks the V3 W6 ShadowDoc compatibility policy to the V2-W3 SwDocShell ApplyPlan path while raising the W6 agent-step-plan self-test to 12 checks, and L219 locks the V3 W6 deterministic Plan-Act-Observe prompt library policy while raising the W6 agent-step-plan self-test to 13 checks without starting gated runtime implementation, ShadowDoc runtime, a new DocShell type, resume runtime, approval UI runtime, Planner runtime, retry loops, scheduler parallelism, prompt runtime, public egress, or archival automation.
- L220 additionally locks the V3 W3 model acquisition policy: BGE-m3 is not bundled by default, is never silently downloaded, hybrid/vector retrieval requires explicit user confirmation, offline/missing/declined model states fall back to SQLite FTS5, public egress stays false by default, and downloader/embedding runtime implementation remains `not-started`.
- L221 additionally locks the V3 W3 vector-store backend policy: sqlite-fts5 remains the default and fallback backend, lancedb-local is opt-in only until macOS arm64 platform smoke exists, `lancedbMacosArm64Status=pending-runtime-spike`, runtime vector-store implementation remains `not-started`, and the W3 chunk self-test now reports 9 checks with 3 valid / 5 invalid fixtures.
- L222 additionally locks the V3 W3 watcher scalability policy: background watcher debounce is 5s, workspaces above 10k files require bounded watcher plus polling fallback, per-file descriptor watch is forbidden, open file descriptors are capped at 256, overflow is `fail-closed-user-visible`, runtime watcher implementation remains `not-started`, and the W3 chunk self-test now reports 10 checks with 3 valid / 6 invalid fixtures.
- L223 additionally locks the V3 W3 extraction policy: PPTX text extraction uses the LibreOffice import filter and Impress document model, standalone PPT parsers are forbidden, slide element refs must be preserved, runtime extraction implementation remains `not-started`, and the W3 chunk self-test now reports 11 checks with 4 valid / 7 invalid fixtures.
- L224 additionally locks the V3 W3 storage policy: index files live under the application data directory as per-workspace sidecars, path identity is `workspace-hash`, user-document colocation/sync is forbidden, runtime storage implementation remains `not-started`, and the W3 chunk self-test now reports 12 checks with 4 valid / 8 invalid fixtures.
- L225 additionally locks the V3 W1 AI workspace UI policy: W1 is a Codex-like `ai-workspace-sidebar`, not modal chat only; it combines conversation, task progress, step list, evidence links, content review, formatting review, DiffReview reuse, before/after layout preview, and openers for document/selection/connector-result/knowledge-index-result/evidence-record/task-step while keeping runtime implementation `not-started`. The W1 self-test reports 9 checks with 5 valid / 28 invalid fixtures at L225.
- L226 additionally locks the V3 W1 content opener route policy: document opens in the main document window; selection, connector-result, knowledge-index-result, and evidence-record open in sidebar-preview; task-step opens in DiffReview; every opener is evidence-linked, read-only, fail-closed-user-visible, and cannot mutate the main document before approval. The W1 self-test now reports 10 checks with 5 valid / 32 invalid fixtures.
- L227 additionally locks the V3 W1 formatting review policy: formatting review is a before/after layout diff, covers paragraph/character/table/cell/slide layout changes, reuses DiffReview, requires evidence links and human approval, stores no raw or preview content in fixtures, and keeps formatting runtime `not-started`. The W1 self-test reports 11 checks with 5 valid / 36 invalid fixtures.
- L228 additionally locks the V3 W1 content review policy: content review is an evidence-linked content diff, covers selection/document-section/connector-result/knowledge-index-result/evidence-record/task-step suggestions, reuses DiffReview, requires evidence links and human approval, stores no raw or suggestion content in fixtures, and keeps content review runtime `not-started`. The W1 self-test now reports 12 checks with 5 valid / 40 invalid fixtures.
- L229 additionally locks the V3 W1 artifact/content navigator policy: W1 must expose a visible navigator for document/selection/connector-result/knowledge-index-result/evidence-record/task-step artifacts, group by type/task, show evidence badges, open through contentOpeners, keep details read-only, store no raw artifact content, and keep artifact navigator runtime `not-started`. The W1 self-test now reports 13 checks with 5 valid / 44 invalid fixtures.
- L230 additionally locks the V3 W1 review queue policy: content-review, formatting-review, and task-step review items must be visible in a queue with queued/open/approved/rejected/applied/failed states, state/type/surface filters, DiffReview opening, evidence links, and explicit human approval for bulk approve/reject. The queue forbids batch auto-apply, raw review content in fixtures, and main-document mutation before approval.
- L231 locks the V3 W1 evidence/citation inspector policy: evidence-record, connector-result, knowledge-index-result, task-step, and review-item sources must be inspectable with citation links, audit trail metadata, content opener integration, redacted raw payloads, hash-only references, evidence links, and visible fail-closed behavior. The inspector forbids raw evidence/citation fixture content, opener bypass, runtime-started claims, and main-document mutation.
- L232 locks the V3 W1 interaction chrome policy: the AI workspace must be a compact native sidebar workbench with segmented tabs for chat/tasks/artifacts/reviews/evidence, persistent composer, visible rails, keyboard tab order, Escape focus return, no focus trap, and visible fail-closed behavior. It forbids modal-only chat, mouse-only navigation, raw UI/content fixture storage, non-native controls, and runtime-started claims.
- L233 locks the V3 W1 content preview matrix policy: document, selection, connector-result, knowledge-index-result, evidence-record, task-step, and review-item content must have predictable read-only preview targets/modes with evidence badges, source metadata, content opener integration, redaction, hash-only references, and visible fail-closed behavior. It forbids raw/preview fixture payloads, opener bypass, premature runtime claims, and main-document mutation. The W1 self-test now reports 17 checks with 5 valid / 60 invalid fixtures.
- L234 locks the V3 W1 workspace action bar policy: the AI workbench must expose visible, keyboard-accessible native commands for preview opening, DiffReview opening, approve/reject, copy-reference, export-evidence, filter/sort, retry, and cancel. It forbids hidden or mouse-only actions, auto-apply, opener/DiffReview bypass, silent failure, premature runtime claims, and main-document mutation. The W1 self-test now reports 18 checks with 5 valid / 64 invalid fixtures.
- L235 locks the V3 W1 workspace filter/search policy: the AI workbench must expose visible metadata-only filtering and search across tasks, artifacts, reviews, evidence, and previews for the current workspace/current document scope. It requires state/type/surface/source/evidence-status filters, id/type/state/source-metadata/evidence-id/hash-reference search fields, hash-only references, content opener and evidence-link reuse, visible fail-closed behavior, no raw content indexing, no cross-document/global index, and no runtime-started claims. The W1 self-test now reports 19 checks with 5 valid / 68 invalid fixtures.
- L236 locks the V3 W1 workspace context handoff policy: opening a filtered result, artifact, review item, evidence link, preview item, or action-bar command must visibly preserve task/evidence/hash/preview/review metadata into preview, DiffReview, evidence inspector, review queue, task progress, and composer targets. It requires breadcrumb, back navigation, focus return, contentOpeners/DiffReview reuse, evidence links, metadata-only/hash-only redaction, no raw handoff payloads, no auto-apply, no main-document mutation, and no runtime-started claims. The W1 self-test now reports 20 checks with 5 valid / 72 invalid fixtures.
- L237 locks the V3 W1 workspace review state sync policy: review queue, DiffReview, preview matrix, evidence inspector, task progress, and action bar must show the same queued/open/approved/rejected/applied/failed state and open/approve/reject/apply/fail transition. It requires evidence links, visible state, contentOpeners/DiffReview reuse, explicit human approval for bulk apply, metadata-only/hash-only redaction, no raw review-state payloads, no auto-apply, no main-document mutation, and no runtime-started claims. The W1 self-test now reports 21 checks with 5 valid / 76 invalid fixtures.
- L238 locks the V3 W1 workspace activity timeline policy: chat, tasks, artifacts, reviews, evidence, previews, and action bar must expose a visible chronological append-only activity trail for requests, tasks, artifacts, opened content, review openings, review-state changes, evidence links, invoked actions, and failures. It requires visible timestamp, visible actor, open target, evidence links, contentOpeners/DiffReview/evidence inspector reuse, metadata-only/hash-only redaction, no raw activity or transcript payloads, no auto-apply, no main-document mutation, and no runtime-started claims. The W1 self-test now reports 22 checks with 5 valid / 80 invalid fixtures.
- L239 locks the V3 W1 workspace session snapshot policy: returning to a document or AI workbench must visibly restore active task, open artifact/review/evidence, preview mode, review state, activity cursor, and failure state for the current workspace/current document. It requires a visible resume summary, explicit resume, timestamp and document binding, contentOpeners/DiffReview/evidence inspector/activity timeline reuse, metadata-only/hash-only redaction, no raw session or transcript payloads, no cross-document restore, no cloud sync, no auto-apply, no main-document mutation, and no runtime-started claims. The W1 self-test now reports 23 checks with 5 valid / 84 invalid fixtures.
- L240 locks the V3 W1 workspace attention routing policy: approval-required, review-ready, task-failed, evidence-missing, and resume-available states must be surfaced through visible workbench badges, row highlights, timeline events, and resume banners that open the owning task, review, DiffReview, evidence inspector, activity timeline, or session snapshot. It requires visible reason, timestamp, open target, keyboard access, native controls, ActionBar/activity/session/evidence/DiffReview reuse, metadata-only/hash-only redaction, no raw attention payloads, no cloud push, no auto-open, no auto-apply, no main-document mutation, and no runtime-started claims. The W1 self-test now reports 26 checks with 5 valid / 96 invalid fixtures.
- L242 locks the V3 W1 workspace content registry policy: document, selection, connector-result, knowledge-index-result, evidence-record, task-step, review-item, formatting-preview, and content-suggestion handles must be registered as metadata-only objects with evidence/hash/open-target/preview-mode metadata before opening, preview, review, evidence inspection, queueing, or resume. It rejects missing registry metadata, type/field/target drift, raw registry payloads, auto-open, auto-apply, main-document mutation, and content registry runtime-started claims. The W1 self-test now reports 26 checks with 5 valid / 96 invalid fixtures.
- `ai-native` counted core/evidence cppunit suite is now 125 cases: 55 provider + 38 cui (8 idx + 8 fuzzy + 10 recent + 7 controller + 5 dispatcher) + 32 W5 lifecycle evidence contracts. Product-surface side suites have current workdir evidence: `sw_apply_engine OK(35)`, `sw_inline_actions OK(10)`, `sc_inline_actions OK(6)`, `sd_inline_actions OK(5)`, and `kqoffice_cowork OK(45)`. B2 harfbuzz Meson `UnicodeDecodeError byte 0xe5` on non-ASCII BUILDDIR `可点office` remains wrapper-resolved at make level via `PKG_CONFIG=/tmp/kqoffice-pkgconf-utf8`.
- V1.5 27/27 strict roundtrip baseline untouched
- 242 ledger rows as of 2026-06-11. L108-L125 moved V2 from source-link/skeleton closure into product-loop closure; L126-L153 promoted product-entry, worker lifecycle, archive boundary, real app/document/UNO dispatch, and GUI readiness gates; L154-L176 built the current-bundle visible proof ladder through resource/process launch, direct launch stabilization, native AX-by-PID attribution, visible Writer menu click-through, CommandPalette UNO/AX, physical Cmd+Shift+K, Cowork Cmd+Shift+T dialog, Writer/Calc/Impress Select-to-act, DiffReview Accept/Reject, real-provider DiffReview click-through, Cowork new-task/accept-task, pending/running rows, live nonblocking transition, macOS native notification proof, Writer/Calc/Impress factory document-window UITest zero-skip, Writer Start Center creation-path UITest coverage, H10 source archive boundary refresh, and explicit source archive batch path lists. L177-L242 add V3 contract-only H8/H9/H10/H11/H12 gates plus W1/W2/W3/W4/W5/W6/W7/W8/W9 meta self-tests while preserving the V2 GA blockers, including the W1 AI workspace UI policy, W1 content opener route policy, W1 formatting review policy, W1 content review policy, W1 artifact navigator policy, W1 review queue policy, W1 evidence inspector policy, W1 interaction chrome policy, W1 content preview matrix policy, W1 workspace action bar policy, W1 workspace filter/search policy, W1 workspace context handoff policy, W1 workspace review state sync policy, W1 workspace activity timeline policy, W1 workspace session snapshot policy, W1 workspace attention routing policy, W1 workspace native style policy, W1 workspace content registry policy, W2 connector trust-chain, read-only/writeback, token-refresh, and auth-flow contracts, the W3 BGE-m3 explicit model acquisition policy, the W3 vector-store default/fallback policy, the W3 watcher scalability policy, the W3 extraction policy, the W3 storage policy, the W5 eval fixture schema, V2 GA reference baseline, LLM-judge reproducibility, report archive-policy locks, W6 forward-only DAG dependency policy, W6 fail-closed plan validation policy, W6 approval UX policy, W6 cross-session resume policy, W6 ShadowDoc compatibility policy, and W6 prompt library policy. Remaining GUI/worker E2E gap is Windows host compile/manual toast proof; source archive commits and D5/D8/B3 release decisions also remain open.
- CI workflow `.github/workflows/v2-contract-harnesses.yml` invokes H1-H7 + fixtures on Ubuntu, H8 only on macOS or when `KDOFFICE_APP_BUNDLE` points at a prebuilt app bundle, and H9 only on macOS or when `KDOFFICE_SRC_ROOT` points at a source tree; pre-L62 only ran 4 and L62-L126 ran 7. Paths-filter covers `bin/v2-harness-sweep.sh` and `.agent/goals/2026-05-08-v2-ai-native/**` (post-L73; previously these inputs could regress without triggering CI).
- Persistent memory seeded at `/Users/lu/.clavue/projects/-Users-lu---office-clmpx2/memory/` (L63):
  `MEMORY.md`, `v2_entry_pointer.md`, `v2_invariants.md`,
  `v2_locking_architecture.md`, `authorization_gates.md`,
  `workspace_layout.md`, `feedback_terse_verifiable.md`
- 4 reader's manuals on disk; H6 glob-discovers them and locks fact-blocks against schema bodies; H2 check 10 locks lane-status references bidirectionally
- L65-L242 session deliverables: see `docs/product/v2/STATUS-2026-05-11.md` §13-§16 and §24-§143, plus ledger.jsonl rows 65-242 for per-row notes. Tier-4 lock chain through check 24 still guards row shape, baseline mirrors, sweep parity, timestamp monotonicity, and cross-doc L-anchor cadence. Future cluster summaries: append a new STATUS section, do not extend this line.

Working tree may have uncommitted noise (sysui/desktop/macosx/LaunchConstraint.plist, generated config). Nothing has been pushed (B3 — no SSH key, HTTPS hits 500MB pack limit).

## What's actually blocked (do NOT keep churning)

| ID | Decision needed | Owner | Blocks |
|---|---|---|---|
| Source归档 | Split and commit `/Users/lu/kdoffice-src` dirty SRCDIR changes by wave (W1/W2/W3/W4/W5/build infra) | user / repo owner | H10 now classifies 175 dirty SRCDIR paths with zero unknowns and 9 split-needed shared build files, and emits explicit `tmp/v2-source-archive-batches/*.paths` lists; actual staging/commit remains open and must not use `git add .`. |
| UITest svp root cause | **RESOLVED L174** — Start Center Writer creation path no longer stalls outside the factory document-window route | V2 UI/test owner | `vcl` UITest completion idle now runs at DEFAULT priority; `UITest_demo_ui` Start Center repro passes, and `UITest_sw_select_to_act` passes with 4 tests / 0 skipped including `create_doc_in_start_center("writer")`. |
| Product-entry static smoke | Installed app-bundle services, registry commands/accelerators, and W2/W4/W5 UI resources | coordinator | **RESOLVED L127** as H8; visible click-through is covered by L163-L169 and full GUI/worker E2E remains open separately. |
| Current-builddir AX window/menu attribution | **RESOLVED L162** — visible current-builddir app pids expose AX windows/menus again | V2 UI/test owner | Current-bundle visible smoke and Cowork visible smoke report `Target attribution: ready` on the exact builddir pid. |
| Full GUI/worker E2E | Prove Windows host compile/manual toast behavior | V2 UI/test owner | H9 is 278 checks; L165 proves visible new-task→awaiting-review→DiffReview→accept-task→applied, L166 proves pending/running rows, L167 proves live nonblocking pending→running, L168 proves macOS native notification submit/dispatch/stored-review-open, and L169 proves real-provider DiffReview click-through. Windows host compile/manual toast proof remains outside this macOS build. |
| Manual product smoke | **RESOLVED L173** — visible Writer/Calc/Impress Select-to-act, DiffReview, real-provider Rewrite, Cowork task-loop, and macOS native notification smokes are automated on the exact current-builddir pid | release/test owner | L147-L173 prove app launch, document processing, installed entry parity, current-bundle PID attribution, CommandPalette, Cowork dialog entry, Writer/Calc/Impress Select-to-act, visible DiffReview Accept/Reject, real Ollama provider evidence, Cowork new-task/accept-task, live pending→running, and macOS native notification proof. Full GUI/worker E2E remains open separately for Windows host toast proof. |
| D5 | Pick `downstream-branding/` source-of-truth (commit / submodule / skip) | user | CI installer artifacts have correct branding. |
| D8 | Decide `sysui/desktop/macosx/LaunchConstraint.plist` `team-identifier` value | user | macOS code signing / notarization. |
| B3 push | Resolve 500MB pack + SSH/LFS/shallow push strategy | user | Sharing/PR path for both builddir and srcdir. |

| Resolved | When | What |
|---|---|---|
| ~~D2 W1 Day-1a OllamaAdapter~~ | L32 (2026-05-10) | Audit found landed: BSD-socket HTTP/1.0, bounded reads, 5 cppunit cases included in OK(84) |
| ~~D7 runtime ApplyPlan schema~~ | L26 (2026-05-10) | `docs/schemas/apply-plan-runtime.schema.json` envelope locked + fixtures; per-kind shape stays open until SwUndoApplyPatch impls land |
| ~~CI 4/7 harness gap~~ | L62 (2026-05-11) | `.github/workflows/v2-contract-harnesses.yml` now invokes all 7; H4/H5/H6/H7 had been silently skipped pre-L62 |
| ~~H8 product-entry gate~~ | L127 (2026-06-08) | `tests/v2-product-entry-smoke-test.sh` added to sweep + CI; installed app-bundle product entrypoints are now locked |
| ~~D1 W3 apply wiring~~ | L108-L122 | `SwDocShell::applyDiagnosticsPlan`, ApplyEngine, undo, parser, and doc-backed apply landed with cppunit evidence |
| ~~D3 W2 Day-1b~~ | L108-L111 | sfx2 dispatcher, cui loader/popover, Cmd+Shift+K, postprocess xcd, and dispatcher cppunit landed |
| ~~D1d W1 honesty pair~~ | L108-L123/L169 | listCapabilities reflection, durationMs, runtime JSON stub, and Ollama JSON-mode request lock landed; provider now OK(55) |
| ~~W4 source/link residual~~ | L108-L120 | Writer/Calc/Impress source-link plus popover/provider/DiffReview/triple-surface apply landed |
| ~~Windows native notification backend~~ | L144 | Windows Shell_NotifyIcon notification-area backend source/build wiring and click dispatch are H9-locked; Windows host compile/manual toast smoke remains part of full GUI/worker E2E |
| ~~Ollama full path~~ | L146/L169 | Local `bin/v2-ollama-real-path-smoke.sh` proves real Ollama `qwen3:0.6b` emits 7-kind W3 runtime JSON; `V2_VISIBLE_DIFF_REVIEW_REAL_PROVIDER=1 bin/v2-visible-diff-review-smoke.sh` proves app-level Rewrite→DiffReview→Accept/Reject through real `ollama: qwen3:0.6b` evidence |
| ~~App launch/init smoke~~ | L147 | `bin/v2-app-launch-smoke.sh` runs H8 static checks, `soffice --version`, and real `--headless --terminate_after_init` with isolated profile + AI env; visible click-through remains open |
| ~~Writer document-processing smoke~~ | L148 | `bin/v2-writer-document-smoke.sh` runs H8 static checks, converts UTF-8 text to ODT through the installed app bundle, stores `tmp/v2-writer-document-output.odt`, and verifies generated `content.xml`; visible click-through remains open |
| ~~Suite document-processing smoke~~ | L149 | `bin/v2-suite-document-smoke.sh` runs H8 static checks and proves Writer txt→ODT, Calc CSV→ODS, Impress ODP→PDF, and Impress PPTX→ODP through the installed app bundle with stable artifacts; visible click-through remains open |
| ~~User-entry chain smoke~~ | L150 | `bin/v2-user-entry-smoke.sh` verifies installed CommandPalette/Cowork commands, Cmd+Shift+K/T shortcuts, Cowork menu entries, DiffReviewDeck, shipped V2 UI controls, and SRCDIR SFX/registry anchors; visible click-through remains open |
| ~~Live UNO dispatch smoke~~ | L151 | `bin/v2-uno-dispatch-smoke.sh` starts the installed app with a UNO socket, opens a hidden Writer frame, queryDispatch-resolves CommandPalette/Cowork/PropertyDeck/DiffReviewDeck, and dispatches non-modal PropertyDeck + CommandPalette; visible click-through remains open |
| ~~Suite-surface UNO dispatch smoke~~ | L152 | `bin/v2-suite-dispatch-smoke.sh` starts the installed app with a UNO socket, opens Writer/Calc/Impress frames, queryDispatch-resolves CommandPalette/Cowork/PropertyDeck/DiffReviewDeck on each surface, and dispatches non-modal PropertyDeck + CommandPalette across all three; visible click-through remains open |
| ~~GUI readiness/install-route parity smoke~~ | L153 | `bin/v2-gui-readiness-smoke.sh` proves osascript/System Events/AX access is available, classifies LaunchServices routing to `/Applications/可圈office.app`, and blocks visible click-through on installed bundle V2 entry-resource parity until the visible app is updated/rerouted |
| ~~Visible current-bundle launch smoke~~ | L154 | `bin/v2-visible-current-bundle-smoke.sh` proved current-bundle V2 entry parity and builddir process launch with `open -n`; L155 supersedes the unbound same-name-process GUI attribution claim |
| ~~Strict current-bundle PID-attribution gate~~ | L155 | `bin/v2-visible-current-bundle-smoke.sh` now passes newly-started builddir pid(s) into System Events and only drives menus/shortcuts when AX exposes that exact pid; current run is `Status: blocked`, 2 passed / 5 blocked / 0 failed |
| ~~Direct-launch PID-attribution stabilization~~ | L156 | `bin/v2-visible-current-bundle-smoke.sh` now starts the current builddir `Contents/MacOS/soffice` directly with an isolated profile, avoiding `open -n`/bundle-id routing ambiguity; current run remains `Status: blocked`, 2 passed / 5 blocked / 0 failed |
| ~~Native AX-by-PID current-bundle menu click-through~~ | L157 | `bin/v2-visible-current-bundle-smoke.sh` now binds the exact builddir pid with `AXUIElementCreateApplication(pid)` and clicks File > New > Text Document on that pid; current run is `Status: blocked`, 6 passed / 1 blocked / 0 failed, with only Cmd+Shift+K shortcut injection still blocked |
| ~~Visible CommandPalette UNO/AX proof~~ | L158 | `bin/v2-visible-current-bundle-smoke.sh` dispatches `.uno:CommandPalette` on the visible Writer frame and verifies CommandPalette/search/results AX nodes on the same builddir pid; current run is `Status: blocked`, `Target attribution: ready`, 8 passed / 1 blocked / 0 failed, with only physical Cmd+Shift+K shortcut injection still blocked inside this smoke |
| ~~Physical Cmd+Shift+K visible CommandPalette proof~~ | L159 | Writer-family module `K_SHIFT_MOD1` en-US overrides now map to `.uno:CommandPalette` instead of `.uno:SmallCaps`; after `gmake officecfg.build postprocess.build`, the current-bundle smoke posts Cmd+Shift+K via native CGEvent and verifies CommandPalette/search/results AX nodes on the exact builddir pid. Current run is `Status: passed`, `Target attribution: ready`, 9 passed / 0 blocked / 0 failed |
| ~~Physical Cmd+Shift+T visible Cowork dialog proof~~ | L160 | `bin/v2-visible-cowork-smoke.sh` launches the current builddir app with an isolated profile, binds the exact builddir pid through native AX-by-PID, posts Cmd+Shift+T via native CGEvent, and verifies `异步任务`, `btn_new_task`, `btn_accept_task`, and `task_list_view` on that pid. Current run is `Status: passed`, `Target attribution: ready`, 5 passed / 0 blocked / 0 failed |
| ~~Visible GUI/AX restoration~~ | L162 | Current app-launch, current-bundle CommandPalette, and Cowork visible smokes all report `Status: passed` with exact builddir pid attribution restored |
| ~~Visible Writer Select-to-act~~ | L163 | `bin/v2-visible-select-to-act-smoke.sh` proves current-builddir pid, visible Writer text selection, `SelectToActPopover` buttons, Rewrite click, and post-click response evidence |
| ~~Visible DiffReview Accept/Reject~~ | L164 | `bin/v2-visible-diff-review-smoke.sh` proves Rewrite-to-DiffReview, document change, Accept undo, and Reject row removal |
| ~~Visible Cowork task loop~~ | L165-L168 | `bin/v2-visible-cowork-smoke.sh` proves new-task→awaiting-review→DiffReview→accept-task→applied, visible pending/running rows, live nonblocking transition, and macOS native notification submit/click/review-open proof |
| ~~Visible DiffReview real Ollama provider~~ | L169 | `V2_VISIBLE_DIFF_REVIEW_REAL_PROVIDER=1 bin/v2-visible-diff-review-smoke.sh` proves visible Rewrite→DiffReview→Accept/Reject through real `ollama: qwen3:0.6b` evidence |
| ~~Writer/Calc/Impress Select-to-act UITests~~ | L170-L174 | `UITest_sw_select_to_act` passes with 4 tests discovered and 0 skipped including Start Center Writer creation; `UITest_sc_select_to_act` and `UITest_sd_select_to_act` pass with 3 tests discovered and 0 skipped through factory document-window loaders |
| ~~Visible Calc/Impress Select-to-act click-through~~ | L173 | `bin/v2-visible-suite-select-to-act-smoke.sh` proves exact-pid Calc `CellRangePopover` and Impress `SlideElementPopover` action buttons, real mouse clicks, and popover-close product response at 16/0/0 |
| ~~Start Center Writer creation-path svp root cause~~ | L174 | `UITest_demo_ui` repro through `create_doc_in_start_center("writer")` passes with `Ran 1 test`, `OK`; `UITest_sw_select_to_act` now includes `test_writer_start_center_create_smoke` and passes with `Ran 4 tests`, `OK`, `Tests skipped: 0` |
| ~~Source archive boundary refresh~~ | L175 | `bin/v2-source-archive-boundary.sh` classifies the L174-expanded W4/source dirty set; H10 passes with 175 dirty paths, 0 unknown paths, 9 split-needed shared paths, and 96 W4-select-to-act paths |
| ~~Source archive explicit batch path lists~~ | L176 | `bin/v2-source-archive-boundary.sh` emits `tmp/v2-source-archive-batches/{W1-provider,W2-command-palette,W3-writer-apply,W4-select-to-act,W5-cowork,build-infra,submodule-dirty,split-needed,unknown}.paths`; H10 locks representative paths, requires `unknown.paths` empty, and now passes with 23 checks |
| ~~V3 H8 connector-manifest contract~~ | L177 | `tests/v3-connector-manifest-contract-test.sh` validates `connector-manifest.schema.json`, 7 valid/3 invalid V3 connector fixtures, W2 spec roster parity, service-mode/evidence/policy semantics, and is wired into `bin/v3-eval-sweep.sh --v3-only` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 H9 eval-baseline seed~~ | L178 | `tests/v3-eval-baseline-test.sh` validates three capability fixtures, one V2 regression fixture, three expected-patch metadata fixtures, V2 token-lock counts, evidence/undo requirements, and is wired into `bin/v3-eval-sweep.sh --v3-only` |
| ~~V3 W5 eval fixture schema lock~~ | L210 | `docs/schemas/eval-capability-fixture.schema.json`, `docs/schemas/eval-expected-patch.schema.json`, and `docs/schemas/eval-regression-fixture.schema.json` are enforced by H9; invalid guards reject runtime-score drift, missing undo preservation, and V2 sweep downgrade; H9 now reports 7 checks and V3 report totals represent 50 contract checks |
| ~~V3 W5 eval reference baseline lock~~ | L211 | H9 capability fixtures now require `referenceBaseline.id=v2-ga-acceptance` with `source=v2-ga`, `versionPolicy=frozen-at-v2-ga`, `frozenAtLedger=L211`, `requiresV2RegressionGreen=true`, and `runtimeReferenceImplementation=not-started`; the invalid W1-v0 baseline guard is enforced, H9 now reports 8 checks, and V3 report totals represent 51 contract checks |
| ~~V3 W5 LLM-judge reproducibility lock~~ | L212 | H9 capability fixtures now require `llmJudgePolicy`, `docs/product/v3/w5-judge-prompt-library.md` locks `judge-v3-capability-v1`, default release gating and nondeterministic parameters are rejected, H9 now reports 9 checks, and V3 report totals represent 52 contract checks |
| ~~V3 W5 report archive policy lock~~ | L213 | `docs/product/v3/w5-report-archive-policy.md` locks per-release JSON/Markdown report archival under `docs/product/v3/eval-reports/<release>/`, keeps heavy artifacts out of git unless release-artifact/LFS is explicitly decided, adds `archivePolicy` to the report schema/sample/template, and raises the W5 report self-test to 10 checks while archive automation remains not-started |
| ~~V3 W6 dependency policy lock~~ | L214 | `docs/product/v3/w6-dependency-policy.md` locks W6 step plans to a forward-only DAG with topological index order, fan-in/fan-out allowed, cycles/future dependencies/runtime parallelism forbidden, and `runtimeSchedulerImplementation=not-started`; W6 agent-step-plan self-test now reports 8 checks with 3 valid and 4 invalid fixtures |
| ~~V3 W6 plan validation policy lock~~ | L215 | `docs/product/v3/w6-plan-validation-policy.md` locks invalid Planner output to fail closed before execution, blocks execution, forbids silent retry and automatic simplification, requires invalid-plan evidence, and keeps `runtimePlannerImplementation=not-started`; W6 agent-step-plan self-test now reports 9 checks with 3 valid and 5 invalid fixtures |
| ~~V3 W6 approval policy lock~~ | L216 | `docs/product/v3/w6-approval-policy.md` locks whole-task approval as the default, allows per-step approval only from explicit user choice, forbids implicit per-step prompts, keeps `runtimeApprovalUiImplementation=not-started`; W6 agent-step-plan self-test now reports 10 checks with 3 valid and 6 invalid fixtures |
| ~~V3 W6 resume policy lock~~ | L217 | `docs/product/v3/w6-resume-policy.md` locks cross-session resume to evidence-complete checkpoints, requires user confirmation, document hash match, shadow snapshot, and audit replay, forbids auto resume, and keeps `runtimeResumeImplementation=not-started`; W6 agent-step-plan self-test now reports 11 checks with 3 valid and 7 invalid fixtures |
| ~~V3 W6 ShadowDoc compatibility policy lock~~ | L218 | `docs/product/v3/w6-shadow-doc-policy.md` locks ShadowDoc compatibility to the V2-W3 SwDocShell ApplyPlan path, forbids a new DocShell type, forbids main-document mutation before approval, and keeps `runtimeShadowDocImplementation=not-started`; W6 agent-step-plan self-test now reports 12 checks with 3 valid and 8 invalid fixtures |
| ~~V3 W6 prompt library policy lock~~ | L219 | `docs/product/v3/w6-prompt-library.md` locks prompt set `w6-plan-act-observe-v1`, Planner/Actor/Observer ids `planner-v1` / `actor-v1` / `observer-v1`, deterministic `temperature=0` / `topP=1` / `seedRequired=true`, no public egress, and prompt runtime `not-started`; W6 agent-step-plan self-test now reports 13 checks with 3 valid and 9 invalid fixtures |
| ~~V3 H10 LocalCloud no-egress contract~~ | L179 | `tests/v3-local-cloud-no-egress-test.sh` validates `localcloud-config.schema.json`, default loopback / enterprise LAN / cloud opt-in fixtures, public-egress guard fixtures, W8 ports 17801-17804, and is wired into `bin/v3-eval-sweep.sh --v3-only` |
| ~~V3 H11 perf-baseline target contract~~ | L180 | `tests/v3-perf-baseline-test.sh` validates `perf-baseline-targets.schema.json`, macOS/Linux/Windows target fixtures, coldStart=2000ms, firstToken=800ms, retrieval=200ms, local Ollama model, 10k top-5 local index, and is wired into `bin/v3-eval-sweep.sh --v3-only` |
| ~~V3 H12 crash-recovery target contract~~ | L181 | `tests/v3-crash-recovery-test.sh` validates `crash-recovery-targets.schema.json`, macOS Writer / Linux Calc / Windows Impress recovery fixtures, autosave=30s, RecoveryDialog=30s, diff=0, local-file-only/no-public-egress recovery semantics, and is wired into `bin/v3-eval-sweep.sh --v3-only` |
| ~~V3 W5 eval-report self-test~~ | L182 | `tests/v3-eval-report-self-test.sh` validates `eval-report.schema.json`, `docs/product/v3/eval-reports/template.md`, sample JSON report fields, H8-H12 command/check totals, no-public-egress/requires-V2-regression gates, and is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W6 agent-step-plan self-test~~ | L183 | `tests/v3-agent-step-plan-test.sh` validates `agent-step-plan.schema.json`, 3 valid/3 invalid W6 fixtures, maxSteps<=25, shadow-doc isolation, dependency order, policy/audit evidence, V2 token-lock preservation, and is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W4 audit-log-entry self-test~~ | L184 | `tests/v3-audit-log-entry-test.sh` validates `audit-log-entry.schema.json`, 3 valid/3 invalid audit fixtures, evidenceId shape, policyDecision coverage, `storesDocumentContent=false`, no evidence-record field embedding, and timestamp-ordered hash-chain continuity; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W3 knowledge-index-chunk self-test~~ | L185/L220/L221/L222/L223/L224 | `tests/v3-knowledge-index-chunk-test.sh` validates `knowledge-index-chunk.schema.json`, 4 valid/8 invalid chunk fixtures, paragraph/sentence-fallback granularity, document/connector source coverage, Writer/Calc/Impress/connector extraction-family coverage, per-workspace scope, local sqlite-fts5 and BGE-m3 1024-dimension hybrid retrieval, `modelAcquisitionPolicy`, `vectorStorePolicy`, `watcherPolicy`, `extractionPolicy`, `storagePolicy`, explicit user confirmation before model download, SQLite FTS5 fallback when BGE-m3 or lancedb-local is missing/unproven, sqlite-fts5 default backend, 5s watcher debounce, bounded watcher plus polling fallback above 10k files, PPTX extraction through LibreOffice import filter + Impress document model, standalone PPT parser forbidden, application-data-directory per-workspace sidecar storage, user-document sync forbidden, no raw document content fields, and no public egress; it reports 12 checks and is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W3 model acquisition policy lock~~ | L220 | `docs/product/v3/w3-model-acquisition-policy.md` locks BGE-m3 as not bundled by default and never silently downloaded; hybrid/vector retrieval requires explicit user confirmation, offline/missing/declined model states fall back to SQLite FTS5, public egress stays false by default, runtime downloader and embedding implementation remain `not-started`, and the W3 chunk self-test now reports 8 checks with 3 valid / 4 invalid fixtures |
| ~~V3 W3 vector-store backend policy lock~~ | L221 | `docs/product/v3/w3-vector-store-policy.md` locks sqlite-fts5 as the default and fallback backend, keeps lancedb-local opt-in only until macOS arm64 platform smoke exists, records `lancedbMacosArm64Status=pending-runtime-spike`, keeps runtime vector-store implementation `not-started`, and raises the W3 chunk self-test to 9 checks with 3 valid / 5 invalid fixtures |
| ~~V3 W3 watcher scalability policy lock~~ | L222 | `docs/product/v3/w3-watcher-scalability-policy.md` locks 5s debounce, bounded watcher plus polling fallback above 10k files, forbids per-file descriptor watch, caps open file descriptors at 256, requires user-visible fail-closed overflow, keeps runtime watcher implementation `not-started`, and raises the W3 chunk self-test to 10 checks with 3 valid / 6 invalid fixtures |
| ~~V3 W3 extraction policy lock~~ | L223 | `docs/product/v3/w3-extraction-policy.md` locks PPTX text extraction to LibreOffice import filter + Impress document model, forbids standalone PPT parsers, requires slide element refs, keeps runtime extraction implementation `not-started`, and raises the W3 chunk self-test to 11 checks with 4 valid / 7 invalid fixtures |
| ~~V3 W3 storage policy lock~~ | L224 | `docs/product/v3/w3-storage-policy.md` locks indexes to application-data-directory per-workspace sidecars, forbids user-document colocation/sync, requires workspace-hash path identity, keeps runtime storage implementation `not-started`, and raises the W3 chunk self-test to 12 checks with 4 valid / 8 invalid fixtures |
| ~~V3 W3 knowledge-index-query/result self-test~~ | L186 | `tests/v3-knowledge-index-query-result-test.sh` validates `knowledge-index-query.schema.json`, `knowledge-index-result.schema.json`, 3 valid/3 invalid paired fixtures, `topK<=10`, query/result linkage, workspace parity, hash-only snippets, no raw query/result content fields, and no public egress; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W4 policy-tenant self-test~~ | L187 | `tests/v3-policy-tenant-test.sh` validates `tenant-context.schema.json`, `policy-rule.schema.json`, 4 valid/3 invalid paired fixtures, tenant isolation, audit sink 17803, effect/enforcement semantics, audit/evidence requirements, and schema-collapse guards; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W6 agent-step-result-state self-test~~ | L188 | `tests/v3-agent-step-result-state-test.sh` validates `agent-step-result.schema.json`, `agent-task-state.schema.json`, 4 valid/3 invalid paired lifecycle fixtures, V2 async cowork scheduling, shadow-doc isolation, `mainDocumentUnchanged`, approval-before-merge, ApplyPlan runtime validation, failure/cancel evidence, and schema-collapse guards; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W7 companion-contract self-test~~ | L189 | `tests/v3-companion-contract-test.sh` validates `companion-pairing-token.schema.json`, `companion-diff-summary.schema.json`, `companion-approval-request.schema.json`, 7 valid/4 invalid fixtures, short pairing token TTL, device binding, read-only diff summaries, no document content storage, online-only biometric approval, LAN push port 17801, and cloud push opt-in guards; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W8 sync-message self-test~~ | L190 | `tests/v3-sync-message-test.sh` validates `sync-message.schema.json`, 4 valid/4 invalid sync fixtures, local-socket loopback and LAN gRPC coverage, hash-only refs, `storesDocumentContent=false`, `containsRawPayload=false`, `ackRequired=true`, `publicEgress=false`, mTLS, W8 sync port 17802, and evidence/audit links; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 onboarding-flow self-test~~ | L191 | `tests/v3-onboarding-flow-test.sh` validates `onboarding-flow.schema.json`, 3 valid/4 invalid first-run fixtures, five ordered steps, download-to-patch `maxMinutes=5`, no-silent-upload/local-first privacy confirmation, skippable local model setup, optional at-most-one connector, demo-patch success/undo/evidence, locale/edition/surface coverage, and runtime-not-started gates; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 starter-pack self-test~~ | L192 | `tests/v3-starter-pack-test.sh` validates `starter-pack-manifest.schema.json`, one full 30-template manifest and four invalid guards, 10 business scenarios, Writer/Calc/Impress counts of 10 each, surface-matched sample patch action kinds, sample patch success/undo/evidence, no-network install, local-only data boundary, W8 self-host compatibility, and runtime-not-started gates; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 edition-policy self-test~~ | L193 | `tests/v3-edition-policy-test.sh` validates `edition-policy.schema.json`, one valid freemium/audit-lock policy and four invalid guards, personal-free ¥0/full local AI, personal-pro ¥39/month, enterprise audit mandatory, enterprise self-hosted W8 deployment, scale-only limits, no feature locks, no audit bypass, and no public-cloud requirement; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 i18n-locale self-test~~ | L194 | `tests/v3-i18n-locale-test.sh` validates `i18n-locale-policy.schema.json`, four valid launch locale manifests and four invalid guards, zh-CN/en-US/ja-JP/zh-TW coverage, UI follows OS locale, AI output follows UI locale, explicit-only `/lang` override, no silent switching/persistence, manual zh-CN/en-US baseline, evidence requirements, and runtime-not-started gates; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 manual-docs self-test~~ | L195 | `tests/v3-manual-docs-test.sh` validates `manual-docs-manifest.schema.json`, one valid manual manifest and four invalid guards, embedded + online mirror policy, `?` help key, Help menu entry, offline-readable zh-CN/en-US manual baseline, eight required topics, no public internet requirement, W8 self-host/release-bundle update path, evidence requirements, and runtime-not-started gates; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 distribution-update self-test~~ | L196 | `tests/v3-distribution-update-test.sh` validates `distribution-update.schema.json`, one valid distribution/update manifest and four invalid guards, DMG/MSI/AppImage/docker first-launch channels, artifact signing/checksum/notarization, installer smoke and download-to-first-patch ≤ 5min, prompt + one-click W8 self-host update, deferrable/no forced update, LAN/no-public-internet policy, rollback proof, evidence requirements, and runtime-not-started gates; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 error-recovery-ux self-test~~ | L197 | `tests/v3-error-recovery-ux-test.sh` validates `error-recovery-ux.schema.json`, one valid recoverable error UX manifest and four invalid guards, inline guidance policy, required next steps, openable evidence, diagnostics export, no dead-end/toast-only drift, main document unchanged until apply, retry/rollback guarantees, and Writer/Calc/Impress/Companion surface coverage; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W9 release-ga-checklist self-test~~ | L198 | `tests/v3-release-ga-checklist-test.sh` validates `release-ga-checklist.schema.json`, one valid GA checklist manifest and four invalid guards, 16 GA readiness gates, V2 regression, H8-H12, W9 onboarding/starter-pack/edition/i18n/manual/distribution/error-recovery gates, source archive cleanup, Windows toast proof, release policy decisions, mandatory human approval, automated approval forbidden, `canShip=false`, signoff evidence, and runtime-not-started gates; it is wired into `bin/v3-eval-sweep.sh --self-test` plus `.github/workflows/v3-contract-harnesses.yml` |
| ~~V3 W1 in-app-chat fixture self-test~~ | L199/L225/L226/L227/L228/L229/L230/L231/L232/L233/L234/L235/L236/L237/L238/L239/L240/L241/L242 | `tests/v3-in-app-chat-test.sh` validates 5 valid / 96 invalid fixtures, Writer/Calc/Impress surface coverage, connector-context coverage, Cmd+Shift+K sfx2-sidebar entry semantics, scoped context autocomplete, Markdown subset rendering, per-doc local history, streaming states, AI workspace UI review/progress/opening semantics, content opener route policy, formatting review policy, content review policy, artifact navigator policy, review queue policy, evidence inspector policy, interaction chrome policy, content preview matrix policy, workspace action bar policy, workspace filter/search policy, workspace context handoff policy, workspace review state sync policy, workspace activity timeline policy, workspace session snapshot policy, workspace attention routing policy, workspace native style policy + workspace content registry policy, V2 Provider/ApplyPlan/evidence reuse, human approval before main-document mutation, no cloud history, no stored prompt content, no raw search indexing, raw handoff payloads, raw review-state payloads, raw activity/transcript payloads, raw session/transcript payloads, or raw attention payloads, raw native style payloads, no new W1 schema, and runtime-not-started gates; it is wired into `bin/v3-eval-sweep.sh --self-test` and reports 26 checks |
| ~~V3 W1 in-app-chat entry-route design~~ | L200 | `docs/product/v3/w1-keyboard-shortcut-survey.md` locks W1 to `Cmd+Shift+K` → V2 CommandPalette → `command-palette-chat-fallback` → `sfx2-sidebar`, `docs/product/v3/w1-sidebar-uiwireframe.md` locks the design-only sidebar states, and `tests/v3-in-app-chat-test.sh` rejects direct accelerator registration with 4 valid / 5 invalid fixtures while preserving no-new-schema and runtime-not-started gates |
| ~~V3 W1 explicit context syntax policy~~ | L201 | `docs/product/v3/w1-context-syntax-policy.md` locks valid mentions to `@selection`, `@doc`, and `@connector:<id>`, requires `defaultScope=none` plus explicit mentions, forbids implicit full-document capture, unknown mentions, connector write-back, and raw content storage, and `tests/v3-in-app-chat-test.sh` now passes with 5 valid / 8 invalid fixtures while preserving no-new-schema and runtime-not-started gates |
| ~~V3 W1 Markdown rendering subset policy~~ | L202 | `docs/product/v3/w1-markdown-rendering-policy.md` locks native-rich-text Markdown rendering to paragraph/heading/list/code-fence/table blocks, forbids WebView/raw HTML/remote images, updates every W1 chat fixture with `output.rendering`, and `tests/v3-in-app-chat-test.sh` now passes with 5 valid / 11 invalid fixtures while preserving no-new-schema and runtime-not-started gates |
| ~~V3 W1 per-document local history policy~~ | L203 | `docs/product/v3/w1-chat-history-policy.md` locks `per-doc-local` + `local-sqlite-sidecar` + `document-id-hash`, forbids cloud sync/global index/cross-document restore/raw transcript fixture storage, requires clear-history/delete-with-document behavior, and `tests/v3-in-app-chat-test.sh` now passes with 5 valid / 15 invalid fixtures while preserving no-new-schema and runtime-not-started gates |
| ~~V3 W1 streaming UI state policy~~ | L204 | `docs/product/v3/w1-streaming-state-policy.md` locks V2 provider chunk source, states idle/requesting/streaming/awaiting-approval/applied/failed/cancelled, append-only chunks, main-document unchanged while streaming, no partial chunk persistence, terminal evidence, and `tests/v3-in-app-chat-test.sh` now passes with 5 valid / 19 invalid fixtures while preserving no-new-schema and runtime-not-started gates |
| ~~V3 W1 context autocomplete/no-conflict policy~~ | L205 | `docs/product/v3/w1-context-autocomplete-policy.md` locks scoped `@` suggestions to `chat-input-only`, delegates existing Office autocomplete controls, gates connector suggestions on W2 manifests, forbids unknown connector suggestions/raw context previews/global autocomplete hijack, and `tests/v3-in-app-chat-test.sh` now passes with 5 valid / 23 invalid fixtures while preserving no-new-schema and runtime-not-started gates |
| ~~V3 W1 AI workspace UI policy~~ | L225 | `docs/product/v3/w1-ai-workspace-ui-policy.md` locks `ai-workspace-sidebar`, `conversation-plus-progress`, visible task progress, step list, evidence links, content review, formatting review, DiffReview reuse, before/after layout preview, and openers for document/selection/connector-result/knowledge-index-result/evidence-record/task-step while rejecting modal-only chat and premature opener runtime drift |
| ~~V3 W1 content opener route policy~~ | L226 | `docs/product/v3/w1-content-opener-policy.md` locks document → main-document-window, selection/connector-result/knowledge-index-result/evidence-record → sidebar-preview, task-step → diff-review, evidence-linked read-only previews, fail-closed visible failure behavior, no main-document mutation before approval, and opener runtime `not-started` |
| ~~V3 W1 formatting review policy~~ | L227 | `docs/product/v3/w1-formatting-review-policy.md` locks before/after layout diff review for paragraph-style, character-style, table-layout, cell-format, and slide-layout changes; it requires DiffReview reuse, evidence links, human approval, no raw/preview fixture content, and formatting runtime `not-started` |
| ~~V3 W1 content review policy~~ | L228 | `docs/product/v3/w1-content-review-policy.md` locks evidence-linked content diff review for selection, document-section, connector-result, knowledge-index-result, evidence-record, and task-step suggestions; it requires DiffReview reuse, evidence links, human approval, no raw/suggestion fixture content, and content review runtime `not-started` |
| ~~V3 W1 artifact navigator policy~~ | L229 | `docs/product/v3/w1-artifact-navigator-policy.md` locks visible artifact/content management for document, selection, connector-result, knowledge-index-result, evidence-record, and task-step artifacts; it requires type/task grouping, evidence badges, content opener integration, read-only details, no raw artifact fixture content, and artifact navigator runtime `not-started` |
| ~~V3 W1 review queue policy~~ | L230 | `docs/product/v3/w1-review-queue-policy.md` locks visible queue management for content-review, formatting-review, and task-step review items; it requires state/type/surface filters, DiffReview opening, evidence links, explicit human approval for bulk actions, no batch auto-apply, no raw review fixture content, and review queue runtime `not-started` |
| ~~V3 W1 evidence inspector policy~~ | L231 | `docs/product/v3/w1-evidence-inspector-policy.md` locks visible evidence/citation inspection for evidence-record, connector-result, knowledge-index-result, task-step, and review-item sources; it requires citation links, audit trail metadata, content opener integration, redacted raw payloads, hash-only references, evidence links, no raw evidence/citation fixture content, and evidence inspector runtime `not-started` |
| ~~V3 W1 interaction chrome policy~~ | L232 | `docs/product/v3/w1-interaction-chrome-policy.md` locks a compact native sidebar workbench with segmented tabs for chat/tasks/artifacts/reviews/evidence; it requires persistent composer, visible rails, keyboard tab order, Escape focus return, no focus trap, no modal-only chat, no raw UI/content fixture storage, and interaction chrome runtime `not-started` |
| ~~V3 W1 content preview matrix policy~~ | L233 | `docs/product/v3/w1-content-preview-matrix-policy.md` locks read-only preview semantics for document, selection, connector result, Knowledge Index result, evidence, task step, and review item content; it requires metadata/read-only/diff/evidence modes, evidence badges, source metadata, redaction, hash-only references, no raw preview payloads, and preview matrix runtime `not-started` |
| ~~V3 W1 workspace action bar policy~~ | L234 | `docs/product/v3/w1-workspace-action-bar-policy.md` locks visible keyboard-accessible native workbench commands for opening previews, opening DiffReview, approving/rejecting, copying references, exporting evidence, filtering/sorting, retrying, and cancelling; it forbids auto-apply, hidden/mouse-only actions, main-document mutation, silent failure, and action bar runtime start claims |
| ~~V3 W1 workspace filter/search policy~~ | L235 | `docs/product/v3/w1-workspace-filter-search-policy.md` locks visible metadata-only filtering and search for tasks, artifacts, reviews, evidence, and previews across current workspace/current document scope; it requires bounded filter/search fields, hash-only references, redacted raw payloads, content opener and evidence-link reuse, no raw content index, no cross-document/global index, visible fail-closed behavior, and filter/search runtime `not-started` |
| ~~V3 W1 workspace context handoff policy~~ | L236 | `docs/product/v3/w1-workspace-context-handoff-policy.md` locks visible context handoff across filter/search, artifact navigator, review queue, evidence inspector, preview matrix, and action-bar open paths; it preserves task/evidence/hash/preview/review metadata, requires breadcrumb/back/focus return, contentOpeners/DiffReview reuse, evidence links, metadata-only hash-only redaction, no raw handoff payloads, no auto-apply, no main-document mutation, visible fail-closed behavior, and context handoff runtime `not-started` |
| ~~V3 W1 workspace review state sync policy~~ | L237 | `docs/product/v3/w1-workspace-review-state-sync-policy.md` locks visible review-state sync across review queue, DiffReview, preview matrix, evidence inspector, task progress, and action bar; it keeps queued/open/approved/rejected/applied/failed states and open/approve/reject/apply/fail transitions aligned, requires evidence links, visible state, contentOpeners/DiffReview reuse, explicit human approval for bulk apply, metadata-only hash-only redaction, no raw review-state payloads, no auto-apply, no main-document mutation, visible fail-closed behavior, and review-state sync runtime `not-started` |
| ~~V3 W1 workspace activity timeline policy~~ | L238 | `docs/product/v3/w1-workspace-activity-timeline-policy.md` locks visible chronological append-only activity history across chat, tasks, artifacts, reviews, evidence, previews, and action bar; it records metadata-only chat/task/artifact/open/review/evidence/action/failure events, requires visible timestamp/actor/open target, evidence links, contentOpeners/DiffReview/evidence inspector reuse, no raw activity/transcript/preview payloads, no auto-apply, no main-document mutation, visible fail-closed behavior, and activity timeline runtime `not-started` |
| ~~V3 W1 workspace session snapshot policy~~ | L239 | `docs/product/v3/w1-workspace-session-snapshot-policy.md` locks visible resume state across chat, tasks, artifacts, reviews, evidence, previews, and activity timeline for current workspace/current document scope; it restores active task, open artifact/review/evidence, preview mode, review state, activity cursor, and failure state, requires resume summary, explicit resume, visible timestamp/document binding, contentOpeners/DiffReview/evidence inspector/activity timeline reuse, metadata-only hash-only redaction, no raw session/transcript/preview payloads, no cross-document restore, no cloud sync, no auto-apply, no main-document mutation, visible fail-closed behavior, and session snapshot runtime `not-started` |
| ~~V3 W1 workspace attention routing policy~~ | L240 | `docs/product/v3/w1-workspace-attention-routing-policy.md` locks visible in-workbench attention routing for approval-required/review-ready/task-failed/evidence-missing/resume-available states through sidebar/tab badges, task-row highlights, review queue badges, activity timeline events, and resume banners; routes open the owning task/review/DiffReview/evidence/timeline/session target with visible reason/timestamp, keyboard access, native controls, metadata-only hash-only redaction, no raw attention payloads, no cloud push, no auto-open, no auto-apply, no main-document mutation, visible fail-closed behavior, and attention routing runtime `not-started` |
| ~~V3 W1 workspace native style policy~~ | L241 | `docs/product/v3/w1-workspace-native-style-policy.md` locks compact native sidebar-workbench styling with segmented tabs, composer/panel/task/artifact/review/evidence/preview/action surfaces, stable toolbar/tab/row/tile dimensions, no text overlap, keyboard access, focus return, no card-pile/modal-only/marketing hero layout, no raw style payloads, visible fail-closed behavior, and native style runtime `not-started` |
| ~~V3 W1 workspace content registry policy~~ | L242 | `docs/product/v3/w1-workspace-content-registry-policy.md` locks metadata-only content handles for document/selection/connector-result/knowledge-index-result/evidence-record/task-step/review-item/formatting-preview/content-suggestion, required evidence/hash/open-target/preview-mode fields, contentOpeners/previewMatrix/evidenceInspector/reviewQueue reuse, no raw registry payloads, no auto-open, no auto-apply, and content registry runtime `not-started` |
| ~~V3 W2 connector manifest trust-chain policy~~ | L206 | `docs/product/v3/w2-manifest-trust-policy.md` locks the manifest trust envelope, makes `trust` required in `connector-manifest.schema.json`, expands H8 to 7 valid / 7 invalid fixtures, rejects unsigned/unhashed/unreviewed/tenant-unapproved manifests, resolves W2 Q4, and keeps H8 at 16 checks without starting connector runtime implementation |
| ~~V3 W2 connector read-only/writeback policy~~ | L207 | `docs/product/v3/w2-connector-operations-policy.md` locks the read-only operations envelope, makes `operations` required in `connector-manifest.schema.json`, expands H8 to 7 valid / 11 invalid fixtures, rejects writeback/write scopes/data-write evidence/runtime write implementation drift, resolves W2 Q5, and keeps H8 at 16 checks without starting connector runtime implementation |
| ~~V3 W2 connector token-refresh policy~~ | L208 | `docs/product/v3/w2-token-refresh-policy.md` locks `auth.refreshPolicy`, makes it required in `connector-manifest.schema.json`, expands H8 to 7 valid / 14 invalid fixtures, rejects background refresh/refresh-token storage/runtime refresh implementation drift, resolves W2 Q3, and keeps H8 at 16 checks without starting connector runtime implementation |
| ~~V3 W2 connector auth-flow policy~~ | L209 | `docs/product/v3/w2-auth-flow-policy.md` locks `auth.flow`, makes it required in `connector-manifest.schema.json`, expands H8 to 7 valid / 17 invalid fixtures, rejects embedded WebView/non-loopback OAuth callback/runtime auth implementation drift, resolves W2 Q1, and keeps H8 at 16 checks without starting connector runtime implementation |

**None of the remaining open rows should be churned as code without a clean verification target.** The next useful work is packaging/commit-boundary cleanup, GUI evidence, or release-policy decisions.

## What does NOT need a new ledger entry

If you're tempted to bump a count field in a doc and append a ledger entry recording the bump — STOP. Turn 8 (L23) already eliminated two such self-referential drift sources (`docs/product/v2-master-plan.md` and `docs/CLAUDE-NOTES.md`). Living counts live ONLY in `docs/product/v2/lane-status.md`. H2 check 9 enforces that lane-status's `(N entries)` claim matches `wc -l` of `ledger.jsonl`.

## Production-ready gate (run before claiming completion)

One-shot sweep (preferred — single source of truth):

```bash
bash bin/v2-harness-sweep.sh                  # H1→H10
bash bin/v2-harness-sweep.sh --with-fixtures  # also V1.5+V2 fixtures w/ ≥36/0 assert
```

Manual equivalent (if you need to inspect individual harness output):

```bash
bash tests/v2-provider-evidence-schema-test.sh       # H1
bash tests/v2-plan-baseline-test.sh                  # H2
bash tests/v2-day0-skeleton-test.sh                  # H3
bash tests/v2-async-task-schema-test.sh              # H4 full
bash tests/v2-inline-action-request-schema-test.sh   # H5 full
bash tests/v2-schema-manual-coherence-test.sh        # H6
bash tests/v2-apply-plan-runtime-schema-test.sh      # H7 full
bash tests/v2-product-entry-smoke-test.sh            # H8 product-entry smoke
bash tests/v2-worker-ui-lifecycle-test.sh            # H9 worker-thread/UI/review-open/OS-notification gateway lifecycle
bash tests/v2-source-archive-boundary-test.sh        # H10 source archive boundary
bash bin/intelligent-contract-fixtures.sh            # V1.5 + V2 fixtures
grep -c '| passed |' tmp/intelligent-contract-fixtures.md  # must be ≥ 36
grep -c '| failed |' tmp/intelligent-contract-fixtures.md  # must be 0
```

Current pass-count baselines: H1=26 / H2=50 / H3=26 / H4 full / H5 full / H6=39 / H7 full / H8=14 / H9=278 / H10=26 / H11=3. ai-native counted core cppunit total = 125; product-surface side suites are listed in §State at handoff.

Or rely on `.github/workflows/v2-contract-harnesses.yml` to run H1-H7 + V1.5 fixtures on PR; H8 requires macOS or a prebuilt app bundle artifact, and H9 requires macOS or a checked-out `KDOFFICE_SRC_ROOT`.

## Coordinator-reversible scope (proceed without asking)

- `docs/` (except `CLAUDE.md`/`AGENTS.md`/`clavue.md` — those are regenerated by `clavue /init`; durable guidance goes in `docs/CLAUDE-NOTES.md`)
- `docs/schemas/` (schemas, fixtures, reader's manuals — H6 auto-enrolls new manuals via glob)
- `tests/` (new harness; preserve 0755 mode bit — H2 check 11 enforces)
- `bin/` (extending validators; do NOT touch `bffvalidator.sh` mode bit)
- `.github/workflows/*.yml` (CI gates)
- `/Users/lu/.clavue/projects/-Users-lu---office-clmpx2/memory/` (persistent memory; outside BUILDDIR, no git pollution)
- `.agent/goals/2026-05-08-v2-ai-native/ledger.jsonl` (append only)
- `.agent/goals/2026-05-08-v2-ai-native/goals.json` (status updates only)

## When in doubt

Pause the goal. Idle turns produce drift, not progress. But check the persistent memory directory FIRST — durable knowledge gaps there are reversible high-value work that isn't visible from inside the BUILDDIR file tree (this was the L62→L63 lesson).
