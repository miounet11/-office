# Packet L10N-02B-A: Pure Visible-Label Slice

Date: 2026-05-07
Owner: Clavue
Reviewer: Codex
Source: `/Users/lu/kdoffice-src` (active source worktree)
Audit basis: `tmp/localization-sweep/l10n-02b-writer-context.md`

## Goal

Stage only the pure user-visible English-to-Chinese label/title/tooltip/accessible-text edits in Writer. No build glue. No feature additions. No control structure changes.

## Allowed source scope

Files where every staged hunk MUST be a `<property name="label|title|tooltip_text|accessible_name|accessible_description|placeholder_text">` text replacement (or equivalent visible text in `strings.hrc`):

| Path | Audit-confirmed visible-string targets |
| --- | --- |
| `sw/inc/strings.hrc` | `STR_LANGSTATUS_NONE`: `None (Do not check spelling)` → `无（不检查拼写）` |
| `sw/uiconfig/swriter/ui/contentcontrollistitemdlg.ui` | dialog title, `Display name:`, `Value:` |
| `sw/uiconfig/swriter/ui/insertautotextdialog.ui` | `Insert AutoText`, `Autotexts for Shortcut` |
| `sw/uiconfig/swriter/ui/insertcaption.ui` | `Insert Caption`, `Properties` |
| `sw/uiconfig/swriter/ui/pbmenubutton.ui` | page-break menu labels |
| `sw/uiconfig/swriter/ui/previewzoomdialog.ui` | dialog title, rows/columns labels with accelerators |
| `sw/uiconfig/swriter/ui/spellmenu.ui` | language menu labels |
| `sw/uiconfig/swriter/ui/tableproperties.ui` | dialog title `Table Properties` → `表格属性` |
| `sw/uiconfig/swriter/ui/tocdialog.ui` | dialog title + preview accessible/visible labels |
| `sw/uiconfig/swriter/ui/translationdialog.ui` | language selection title/label |
| `sw/uiconfig/swriter/ui/selectaddressdialog.ui` | address list title/description/buttons/column titles |
| `sw/uiconfig/swriter/ui/selectblockdialog.ui` | address block title/buttons/options/section label |
| `sw/uiconfig/swriter/ui/sidebarwrap.ui` | wrap/sidebar labels and tooltips |
| `sw/uiconfig/swriter/ui/titlepage.ui` | dialog labels, accessible descriptions (stock buttons require shared-stock review) |
| `sw/uiconfig/swriter/ui/watermarkdialog.ui` | dialog title, field labels, accessible descriptions (stock buttons require shared-stock review) |
| `sw/uiconfig/swriter/ui/wordcount-mobile.ui` | mobile word count labels |
| `sw/uiconfig/swriter/ui/wordcount.ui` | word count labels |

**Excluded from scope on purpose:** `sw/uiconfig/swriter/ui/notebookbar.ui` and `sw/uiconfig/swriter/ui/notebookbar_groupedbar_compact.ui`. These were `partial candidate` in the audit because the same diff carries underline/control-removal hunks. Their localizable substrings move into a separate slice once Packet C lands first or runs in parallel under stricter review.

## Explicitly excluded (non-goals)

- Any change to `sw/Library_sw.mk`, `sw/inc/docsh.hxx`, `sw/qa/core/uwriter.cxx` (belongs to FEAT-WRITER-ANALYZER-CARVE).
- Any change to `sw/uiconfig/swriter/ui/notebookbar*.ui` (belongs to L10N-02B-C-BEHAVIOR or its localizable substring follow-up).
- Renaming or translating: UNO command ids, XML widget ids, schema nodes, real font names, internal style identifiers.
- Stock button text edits (`Open`, `OK`, `Cancel`) — these come from shared stock strings; do not override per-dialog.
- Any structural `.ui` change: adding/removing widgets, reordering containers, changing `class=`, `id=`, `action-name=`, `column=`/`row=`, accelerators that change keyboard semantics.

## Required commands

```sh
gmake -C /Users/lu/kdoffice-src sw.build
gmake -C /Users/lu/kdoffice-src sw.unitcheck
```

Optional after build: relaunch packaged app and rerun `bin/packaged-screenshots.sh` to confirm visual.

## Required evidence

Write to `tmp/localization-sweep/l10n-02b-a.md`:

- `git diff --stat` of staged files (must touch only files in the scope table)
- Every translated string with before/after
- Any English deliberately preserved with reason (technical term / real font / id / fixture / stock button)
- Output of the two `gmake` commands (status + last 20 lines)
- Optional: link to refreshed Writer screenshot

## Stop conditions

Abort and report `Blocked` if:

1. `git diff` for any in-scope file shows a non-text-property hunk (any `id=`, `action-name=`, structural change).
2. Any out-of-scope file from the IntelligentWriterAnalyzer or notebookbar set appears in the staging set.
3. `sw.build` or `sw.unitcheck` fails on a string that was changed in this packet.
4. A staged string maps to a UNO command id, XML schema node, or real font name.
5. The two `notebookbar*.ui` partial candidates accidentally enter the staging set.
