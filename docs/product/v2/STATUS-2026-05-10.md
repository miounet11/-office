# 可圈office V2 当前状态总览

> **生成时间**: 2026-05-10
> **生成者**: Clavue (Opus 4) — 应用户 "梳理当前程序全部情况" 请求
> **数据源**: `lane-status.md` / `ledger.jsonl` / `git log` / `goals.json` / 实时仓库状态
>
> 这是一份**状态快照**（read-only），不包含新决策。决策入口仍在
> `lane-status.md §Open decisions`。

---

## 1. 一句话总览

V1.5 已完工并打 milestone；V2 (AI-native office) 处于 **W2 Day-1b 推进中**，
具体卡在 **Cmd+K 快捷键冲突** —— 需要用户拍板 A/B 方案。其他 wave (W3 部分、
W4/W5 spec-only) 等待 D1/D6 授权。

---

## 2. 仓库形态

```
BUILDDIR  /Users/lu/可点office          # 配置过的 LibreOffice 风格构建树
SRCDIR    /Users/lu/kdoffice-src        # 源码 git checkout
INSTDIR   instdir/可圈office.app        # macOS app bundle 输出位
WORKDIR   workdir/                      # gbuild 中间产物
```

- BUILDDIR 顶层 `Makefile` 是 autogen 生成的，模块快捷键如
  `make sw.build` / `make sc.build` 走 `solenv/gbuild/partial_build.mk`。
- `instdir/` 现状: `Contents/` 存在但近乎空 → 没有可运行的 .app。
- `workdir/` 有目录结构 (CppunitTest/CxxObject/...) 但是 fresh state，
  非全量构建产物。
- `dictionaries` / `helpcontent2` 是 submodule，本次任务无关。

---

## 3. 工具链

| 项 | 值 |
|---|---|
| 平台 | macOS Darwin 24.6.0 (arm64) |
| Xcode | 26.3 |
| macOS SDK | 26.2 |
| min target | 11.0 |
| GNU Make | 4.4.1 (`/opt/homebrew/bin/gmake`) |
| pkg-config | pkgconf 2.5.1 (`/opt/homebrew/bin/pkg-config`) |
| 模型 | Clavue Opus 4 (`claude-opus-4-7`) |

---

## 4. 当前 git 状态

### BUILDDIR (`/Users/lu/可点office`) — branch `main`

```
HEAD  a3a0fe0  docs: D1/D3 source-tree audit, commit decision close-out (L33)
       947e9e0  docs: correct W1 Day-1a audit status (L32)
       1a3d014  CI installer: add zh-CN lang + bundle-id (I2 partial fix)
       5791808  V2 narratives + ledger + handoff + manifest
       1b417cb  V2 contract harness baseline: 4 harness + schemas + fixtures + CI
```

dirty: 几乎全是 `instdir/` / `workdir/` / `autom4te.cache/` 生成物 — 不会
被 commit (按 memory 规则永远只用显式路径 add)。

### SRCDIR (`/Users/lu/kdoffice-src`) — branch `main`, ahead origin **17**

```
HEAD  de0fd779f  feat(sfx2): register SID_COMMAND_PALETTE slot for V2 W2 Cmd+K     # D3.1
       21ecc7966  .gitignore: ignore local /.claude /.clavue agent metadata
       1ad4e428f  V1.5 i18n tail: include/sfx2 strings + openoffice.lst + reportdesign menubar
       4842c8fb7  V1.5 cross-module: zh-CN i18n sweep + menubar ordering + branding outreach
       ...
       0b86a28a2  V2 W3 + sd: IntelligentWriterAnalyzer (preview-only) + PresentationOutline
       49e075307  V2 W2: Cmd+K palette CommandIndex + FuzzyMatcher + RecentStore
       1b0c10459  V2 W1: Provider runtime + Ollama adapter + evidence/apply-plan validator
       ba64c0838  V2 W1+W2 Day-0: provider skeleton + Cmd+K fuzzy matcher
```

**未推远端** (F2 决策: 无 SSH key + HTTPS 2GB pack 限制 500MB)。

未 commit 改动:
- `officecfg/.../GenericCommands.xcu` +11 行 → 本次刚加的
  `.uno:CommandPalette` label node (D3.3a)。

---

## 5. 路线图与各 Wave 状态

### V1.5 ✅ 已完工 (milestone `2026-05-08-v1.5-completion`)

- zh-CN i18n 全套 (vcl / sw / sc / sd / cui / sfx2 / desktop / framework / ...)
- 商务模板 + 可圈office 品牌资产
- macOS web/template iconset 刷新
- workbench UITest smoke 套件
- IconHelper / tray / sidebar 视觉精修
- 27/27 strict roundtrip baseline 锁定

### V2 (`2026-05-08-v2-ai-native`) — AI-native office

| Wave | 焦点 | 状态 |
|---|---|---|
| W1 | Provider runtime (Ollama-first 沙箱) | 主体已落地 + 51 cppunit |
| W2 | Cmd+K 命令调色板 | **进行中 — D3.1 commit, D3.2 跳过, D3.3 卡冲突** |
| W3 | Writer apply-runtime + ApplyPlan validator | Day-1{a,c-h} ✅, **Day-1b ⛔ 待 D1 授权** |
| W4 | Select-to-act (Writer/Calc/Impress 操作气泡) | spec-only |
| W5 | Async cowork (长任务 + diff review) | spec-only |

**ai-native cppunit 总数**: **77** cases (51 provider + 26 cui)。

### W1 Provider Runtime — 详情

| Step | 状态 | 关键文件 | Notes |
|---|---|---|---|
| Day-1 step3 | ✅ | `kqoffice/Library_kqoffice_ai.mk` | 真 UNO 组件 |
| Day-1a | ✅ | `OllamaAdapter.{hxx,cxx}`, `Provider.cxx` | 真 BSD-socket HTTP/1.0 → `127.0.0.1:11434`，100ms 探测 + 30s 生成超时，bounded read (8KB tags / 256KB generate)，5 cppunit |
| Day-1b | ✅ | `EvidenceRecorder.{hxx,cxx}` | 16-hex `evidence_id`, JSON envelope 锁定 |
| Day-1c | ⛔ | i18npool 线程化 | 触及共享 i18n 表面，待授权 |
| Day-1d | ⛔ | 云模式 TLS | 离线路径稳定前不开 |

### W2 Cmd+K — 详情 (本次焦点)

| Step | 状态 | 关键文件 |
|---|---|---|
| Day-1a | ✅ | `cui/source/inc/commandpalette/CommandIndex.hxx`, `qa/unit/{CommandIndex,FuzzyMatcher}Test.cxx` (8+8 cases) |
| RecentStore | ✅ | `RecentStore.hxx` + 10 cases (JSON 往返 + 排序) |
| Controller | ✅ partial | `cui/source/dialogs/commandpalette/CommandPalette.cxx` (29 行 thin wrapper) |
| **D3.1** | ✅ commit `de0fd779f` | sfx2 sdi 注册 `SID_COMMAND_PALETTE = SID_SFX_START + 1753` |
| **D3.2** | ⏸ 跳过 | popover hosting 卡 sfx2→cui 反向依赖 + 无法编译验证；正确路径是 `SfxAbstractDialogFactory::CreateCommandPalette` 接口扩展 |
| **D3.3a** | ✅ 编辑完 | `GenericCommands.xcu` 加 `.uno:CommandPalette` node + zh-CN/en-US label |
| **D3.3b** | 🔴 **卡冲突** | `K_MOD1` (Cmd+K) 已绑 `.uno:HyperlinkDialog` |
| D3.3c | 待办 | commit message 已拟好 |
| D3.4 | 待办 | CppunitTest_cui_commandpalette + UITest_commandpalette |

### W3 Writer Apply Runtime — 详情

| Step | 状态 | Notes |
|---|---|---|
| Day-1a | ✅ | `ApplyPlanValidator.hxx` header-only, 14/14 ValidationCode |
| Day-1b | ⛔ | `SwDocShell::applyDiagnosticsPlan` wiring (待 **D1** 授权) |
| Day-1c | ✅ | `applyPlanValidationMessage` 14 zh-CN toast strings |
| Day-1d | ✅ | parser primitives micro-tests +5 |
| Day-1e | ✅ | `applyPlanValidationStatus` 14-token ASCII kebab 锁定 +3 |
| Day-1f | ✅ | `provider-evidence.schema.json` (JSON Schema 2020-12) + 3 fixtures |
| Day-1g | ✅ | UTF-8 multi-codepoint 边界 fixtures +5 |
| Day-1h | ✅ | `tests/v2-provider-evidence-schema-test.sh` schema↔C++ drift lock |

---

## 6. 当前阻塞 (ordered by 紧急度)

### 🔴 B1 — Cmd+K 快捷键冲突 (本次任务)

`Accelerators.xcu:93` `K_MOD1` 已绑定 `.uno:HyperlinkDialog`。

**待用户拍板 A/B**:
- **A**: 抢占 K_MOD1 → CommandPalette。代价: HyperlinkDialog 失去 Cmd+K（Word/Pages/Google Docs 全行业 Cmd+K=Hyperlink）。
- **B** (推荐): CommandPalette → MOD1_SHIFT+K (Cmd+Shift+K)。代价: 偏离 spec，但零回归。

### 🟡 B2 — 无法本地编译验证

`sfx2.build` 失败 (`concat-deps: No such file or directory` +
`gb_Deliver_deliver: ... libsal_textenclo.dylib ...`)。
BUILDDIR `workdir` 841M 但 fresh state、`instdir` 几乎空。

→ **影响**: D3.1 / D3.3 落地后无法在本机跑 svidl + 全量编译验证。
依赖未来有 build infra 的 session 补 verify。

### 🟢 B3 — 远端推送被禁

F2 决策: 无 SSH key + HTTPS 2GB pack 触 500MB 限制。
SRCDIR ahead origin **17** commits，全部本地。

### ⚪ B4 — 上层授权未到

| # | 待授权 | 影响 |
|---|---|---|
| D1 | W3 Day-1b SwDocShell wiring | 关闭 W3 最后一道 Day-1 关 |
| D6 | worker owned-paths 重新 scope (`src/**` → `<module>/source/**`) | code-worker-2 / test-worker-2 |
| 分支 | `downstream-branding/` source of truth | CI 静默 build without branding |

---

## 7. 三条铁律 (本会话约束)

1. **本地优先 (Ollama)**
2. **不静默上传**
3. **AI 输出对象级 patch**

合规授权清单 (clavue.md): `sfx2 sdi` / `officecfg` / `cui commandpalette` /
`sw docsh`。其他模块改动需重新授权。

---

## 8. Drift Locks (programmatic, last green 2026-05-09 / 2026-05-10)

| Lock | 验证脚本 |
|---|---|
| Schema status enum ↔ C++ apply-plan-* 13/13 | `tests/v2-provider-evidence-schema-test.sh` |
| 防御性 `apply-plan-unknown` 在 C++、不在 schema | 同 |
| `Provider.cxx` runtime token ⊆ schema runtime enum | 同 |
| 9-key `EvidenceRecorder` envelope shape | 同 |
| `ApplyPlanValidator.hxx` 14-tag enum | `testApplyPlanCodeEnumStable` |
| `applyPlanValidationStatus` distinct-per-code | `testApplyPlanStatusDistinctPerCode` |
| `applyPlanValidationMessage` distinct-per-code | `testApplyPlanMessageDistinctPerCode` |
| V1.5 27/27 strict roundtrip | `bin/intelligent-contract-fixtures.sh` |

`tests/v2-plan-baseline-test.sh` 当前 baseline:
**Fixtures 26 across 11 schemas / Checks 21 / passed**（2026-05-10 实跑确认）。

---

## 9. 接下来三步 (按 B1 拍板分支)

### 若选 B (推荐)

```
1. 编辑 Accelerators.xcu 的 PrimaryKeys/Global 节点
   加  K_SHIFT_MOD1   →   .uno:CommandPalette
2. 同步更新 docs/product/v2/w2-cmd-palette-spec.md "Accelerator" 段
3. SRCDIR commit:
     feat(officecfg): register .uno:CommandPalette label + Cmd+Shift+K accelerator for V2 W2
4. 进 D3.4: CppunitTest_cui_commandpalette + UITest_commandpalette
```

### 若选 A

```
1. 编辑 Accelerators.xcu 的 K_MOD1 node 把 .uno:HyperlinkDialog 改 CommandPalette
   并把 HyperlinkDialog 迁移到 K_SHIFT_MOD1（避免直接丢功能）
2. README/spec 写明 Cmd+K 快捷键变更（用户教育）
3. SRCDIR commit (同上 message 拼成一条)
4. D3.4 同 B 分支
```

---

## 10. 文档地图

权威性顺序（V2 改动前必读）:

1. `docs/CLAUDE-NOTES.md` — durable guidance（不会被 /init 覆写）
2. `docs/product/v2/lane-status.md` — 实时 wave 状态
3. `.agent/goals/2026-05-08-v2-ai-native/ledger.jsonl` — append-only 时间线 (33 行)
4. `docs/product/v2/day1-progress.md` — Day-1{a..h} per-step rationale
5. `docs/product/v2/w[1-5]-*-spec.md` — wave 设计 spec
6. `docs/product/v2-master-plan.md` — V2 总图

辅助:
- `docs/schemas/*.schema.json` — 9-10 schemas (provider-request /
  provider-evidence / apply-plan-runtime / evidence-record / apply-plan ...)
- `docs/schemas/fixtures/` — 24 fixtures
- `docs/v2-commit-manifest-2026-05-10.md` — V2 commit 清单
- `docs/v2-coordinator-handoff-2026-05-10.md` — 8-worker sweep 交接

---

## 11. Memory 索引 (auto-memory)

`/Users/lu/.clavue/projects/-Users-lu---office/memory/MEMORY.md`:

- 世界级质量目标
- ICC peer terminal 提交验证
- Build artifacts in git index (永不 `git add .`)
- V2 AI-native entry pointer (改动前阅读顺序)
- V2 hard invariants (Schema↔C++ token parity / 防御 unknown / Provider.cxx subset / V1.5 baseline / 命名 — harness 锁定)
- /goal mode coordinator churn detection

---

**梳理完成。等你拍板 B1 (A 还是 B)，立刻继续。**
