# 可圈office V2 当前状态总览

> **生成时间**: 2026-05-10（L41 落定后刷新）
> **生成者**: Clavue (Opus 4)
> **数据源**: `lane-status.md` / `ledger.jsonl` / `git log` / `goals.json` / 实时仓库状态
>
> 这是一份**状态快照**（read-only），不包含新决策。当前决策入口在
> `lane-status.md §"Current L126 blockers"`。

---

## 1. 一句话总览

V1.5 已完工并打 milestone；V2 (AI-native office) 在本会话完成了 **W2 S4
controller** 落地和 **W5 schema + fixtures + harness** 的无授权部分。
当前阻塞点：**W2 D3.3b Cmd+K 冲突**（B1，A/B 待拍）、**W3 Day-1b** /
**W4 Day-0** / **W5 Day-0 C++** 等授权（D1/Scope）、**B2** 本地无法编译、
**F2** 推送被禁。四个 V2 contract harness 全绿。

---

## 2. 仓库形态

```
BUILDDIR  /Users/lu/可点office          # 配置过的 LibreOffice 风格构建树
SRCDIR    /Users/lu/kdoffice-src        # 源码 git checkout
INSTDIR   instdir/可圈office.app        # macOS app bundle 输出位
WORKDIR   workdir/                      # gbuild 中间产物
```

- BUILDDIR 顶层 `Makefile` 是 autogen 生成的，模块快捷键如
  `make sw.build` / `make sc.build` 走 `solenv/gbuild/partial_build.mk`。
- `instdir/` 现状: `Contents/` 存在但近乎空 → 没有可运行的 .app。
- `workdir/` 有目录结构 (CppunitTest/CxxObject/...) 但是 fresh state，
  非全量构建产物。
- `dictionaries` / `helpcontent2` 是 submodule，本次任务无关。

---

## 3. 工具链

| 项 | 值 |
|---|---|
| 平台 | macOS Darwin 24.6.0 (arm64) |
| Xcode | 26.3 |
| macOS SDK | 26.2 |
| min target | 11.0 |
| GNU Make | 4.4.1 (`/opt/homebrew/bin/gmake`) |
| pkg-config | pkgconf 2.5.1 (`/opt/homebrew/bin/pkg-config`) |
| 模型 | Clavue Opus 4 (`claude-opus-4-7`) |
| clang | Apple clang 17.0.0 |

---

## 4. 当前 git 状态

### BUILDDIR (`/Users/lu/可点office`) — branch `main`

```
HEAD  1792e0a  schemas: V2 W5 async-task schema + fixtures + H4 partial-enforce (L41)
       94eaee6  tests: H2 grows 21 → 26 checks; structural V2 plan locks (L40)
       a940134  tests: v2-async-task-schema-test.sh skeleton harness for W5 Day-0 (L39)
       12381f3  docs: lane-status mirror catch-up for L37 + entry count (L38)
       782a618  docs: V2 W4/W5 spec gain explicit enum-lock tables (L37)
       5c78fe4  docs: D6 worker-scope mapping for /parallel (partial resolution, L36)
       96e5353  docs: V2 W2 S4 close-out + W4/W5 Day-0 entry plans (L34/L35)
       a3a0fe0  docs: D1/D3 source-tree audit, commit decision close-out (L33)
       947e9e0  docs: correct W1 Day-1a audit status (L32)
       1a3d014  CI installer: add zh-CN lang + bundle-id (I2 partial fix)
```

dirty: 几乎全是 `instdir/` / `workdir/` / `autom4te.cache/` 生成物 — 不会
被 commit（按 memory 规则永远只用显式路径 add）。当前还有
`sysui/desktop/macosx/LaunchConstraint.plist` team-identifier 被清空的
修改，未确认是否有意（B2 后续复核项）。

### SRCDIR (`/Users/lu/kdoffice-src`) — branch `main`, ahead origin **18**

```
HEAD  8fe469f71  feat(cui): inline CommandPaletteController + add controller cppunit (V2 W2 S4)
       de0fd779f  feat(sfx2): register SID_COMMAND_PALETTE slot for V2 W2 Cmd+K     # D3.1
       21ecc7966  .gitignore: ignore local /.claude /.clavue agent metadata
       1ad4e428f  V1.5 i18n tail: include/sfx2 strings + openoffice.lst + reportdesign menubar
       ...
       0b86a28a2  V2 W3 + sd: IntelligentWriterAnalyzer (preview-only) + PresentationOutline
       49e075307  V2 W2: Cmd+K palette CommandIndex + FuzzyMatcher + RecentStore
       1b0c10459  V2 W1: Provider runtime + Ollama adapter + evidence/apply-plan validator
       ba64c0838  V2 W1+W2 Day-0: provider skeleton + Cmd+K fuzzy matcher
```

**未推远端** (F2 决策: 无 SSH key + HTTPS 2GB pack 限制 500MB)。

未 commit 改动: `officecfg/.../GenericCommands.xcu` +11 行 →
`.uno:CommandPalette` label node (D3.3a)。仍在 SRCDIR 待 B1 拍板后
并入单个 accelerator+label commit。

---

## 5. 路线图与各 Wave 状态

### V1.5 ✅ 已完工 (milestone `2026-05-08-v1.5-completion`)

- zh-CN i18n 全套 (vcl / sw / sc / sd / cui / sfx2 / desktop / framework / ...)
- 商务模板 + 可圈office 品牌资产
- macOS web/template iconset 刷新
- workbench UITest smoke 套件
- IconHelper / tray / sidebar 视觉精修
- 27/27 strict roundtrip baseline 锁定

### V2 (`2026-05-08-v2-ai-native`) — AI-native office

| Wave | 焦点 | 状态 |
|---|---|---|
| W1 | Provider runtime (Ollama-first 沙箱) | 主体已落地 + 51 cppunit |
| W2 | Cmd+K 命令调色板 | Controller cppunit 落地（S4）；**D3.3b 卡 Cmd+K 冲突等 B1 拍板** |
| W3 | Writer apply-runtime + ApplyPlan validator | Day-1{a,c-h} ✅, **Day-1b ⛔ 待 D1 授权** |
| W4 | Select-to-act (Writer/Calc/Impress 操作气泡) | spec + Day-0 plan + enum lock (L37) |
| W5 | Async cowork (长任务 + diff review) | spec + Day-0 plan + token lock (L37) + **schema + fixtures + H4 partial-enforce (L41)** |

**ai-native cppunit 总数**: **84** cases
（51 provider + 33 cui = 8 idx + 8 fuzzy + 10 recent + 7 controller）。

### W1 Provider Runtime — 详情

| Step | 状态 | 关键文件 | Notes |
|---|---|---|---|
| Day-1 step3 | ✅ | `kqoffice/Library_kqoffice_ai.mk` | 真 UNO 组件 |
| Day-1a | ✅ | `OllamaAdapter.{hxx,cxx}`, `Provider.cxx` | 真 BSD-socket HTTP/1.0 → `127.0.0.1:11434`，100ms 探测 + 30s 生成超时，bounded read (8KB tags / 256KB generate)，5 cppunit |
| Day-1b | ✅ | `EvidenceRecorder.{hxx,cxx}` | 16-hex `evidence_id`, JSON envelope 锁定 |
| Day-1c | ⛔ | i18npool 线程化 | 触及共享 i18n 表面，待授权 |
| Day-1d | ⛔ | 云模式 TLS | 离线路径稳定前不开 |

### W2 Cmd+K — 详情

| Step | 状态 | 关键文件 |
|---|---|---|
| Day-1a | ✅ | `cui/source/inc/commandpalette/CommandIndex.hxx`, `qa/unit/{CommandIndex,FuzzyMatcher}Test.cxx` (8+8 cases) |
| RecentStore | ✅ | `RecentStore.hxx` + 10 cases (JSON 往返 + 排序) |
| Controller (S4) | ✅ | `CommandPalette.hxx` header-only inline 模仿 FuzzyMatcher fdo#47246；`CommandPalette.cxx` 29 行 placeholder TU 保留给 Day-1b popover glue；`qa/unit/CommandPaletteControllerTest.cxx` 7 cppunit cases（SRCDIR commit `8fe469f71`） |
| **D3.1** | ✅ | SRCDIR commit `de0fd779f` sfx2 sdi 注册 `SID_COMMAND_PALETTE = SID_SFX_START + 1753` |
| **D3.2** | ⏸ 跳过 | popover hosting 卡 sfx2→cui 反向依赖 + 无法编译验证；正确路径是 `SfxAbstractDialogFactory::CreateCommandPalette` 接口扩展 |
| **D3.3a** | ✅ 编辑完 | `GenericCommands.xcu` 加 `.uno:CommandPalette` node + en-US label（zh-CN 走 .po 翻译流不内嵌） |
| **D3.3b** | 🔴 **卡 B1** | `K_MOD1` (Cmd+K) 已绑 `.uno:HyperlinkDialog` |
| D3.3c | 待办 | commit message 已拟好 |
| D3.4 | 待办 | CppunitTest_cui_commandpalette + UITest_commandpalette（D3 授权到位后才能添加 popover） |

### W3 Writer Apply Runtime — 详情

| Step | 状态 | Notes |
|---|---|---|
| Day-1a | ✅ | `ApplyPlanValidator.hxx` header-only, 14/14 ValidationCode |
| Day-1b | ⛔ | `SwDocShell::applyDiagnosticsPlan` wiring（待 **D1** 授权） |
| Day-1c | ✅ | `applyPlanValidationMessage` 14 zh-CN toast strings |
| Day-1d | ✅ | parser primitives micro-tests +5 |
| Day-1e | ✅ | `applyPlanValidationStatus` 14-token ASCII kebab 锁定 +3 |
| Day-1f | ✅ | `provider-evidence.schema.json` (JSON Schema 2020-12) + 3 fixtures |
| Day-1g | ✅ | UTF-8 multi-codepoint 边界 fixtures +5 |
| Day-1h | ✅ | `tests/v2-provider-evidence-schema-test.sh` schema↔C++ drift lock |

### W4 Select-to-act — 详情

Spec + Day-0 Entry-Point Plan + **Action enum lock** (L37)。
C++ 未落；Day-0 gated on scope auth for
`sw/source/uibase/inline-actions/` / `sc/source/ui/inline-actions/` /
`sd/source/ui/inline-actions/` / `svx/source/sidebar/diff-review/`。

Locked tokens (spec §"Action enum lock"):
- `ParagraphAction` — 7 tokens: `rewrite | expand | shorten | translate-en | format-clean | explain | custom`
- `CellAction` — 5 tokens: `explain-data | suggest-chart | generate-formula | format-clean | format-change`
- `SlideElementAction` — 4 tokens: `rewrite-text | adjust-color | relayout | translate-text`

### W5 Async cowork — 详情

Spec + Day-0 Entry-Point Plan + **Token lock** (L37) + **schema**
(`docs/schemas/async-task.schema.json`，14 props / 11 required /
`additionalProperties:false`, L41) + **2 fixtures** (`async-task.{valid,invalid}.json`, L41) +
**H4 partial-enforce** 模式。C++ 仍 gated（`kqoffice/source/ai/cowork/**`
+ `kqoffice/qa/cppunit/test_cowork*`）。

Locked tokens (spec §"Token lock"):
- `TaskKind` — 4 tokens: `weekly-report | outline-to-slides | contract-review | data-cleanup`（一 scenario 一 token）
- `TaskState` — 6 tokens: `pending | running | awaiting-review | applied | failed | cancelled`（**不是** `needs-review` / `rejected`）

H4 当 `${KDOFFICE_SRC_ROOT}/kqoffice/source/ai/cowork/AsyncTask.hxx`
出现时自动升级到 full-enforce 模式。

---

## 6. 当前阻塞 (ordered by 紧急度)

### 🔴 B1 — Cmd+K 快捷键冲突

`Accelerators.xcu:93` `K_MOD1` 已绑定 `.uno:HyperlinkDialog`。

**待用户拍板 A/B**:
- **A**: 抢占 K_MOD1 → CommandPalette。代价: HyperlinkDialog 失去 Cmd+K（Word/Pages/Google Docs 全行业 Cmd+K=Hyperlink）。
- **B** (推荐): CommandPalette → MOD1_SHIFT+K (Cmd+Shift+K)。代价: 偏离 spec，但零回归。

### 🟡 B2 — 无法本地编译验证

`sfx2.build` 失败 (`concat-deps: No such file or directory` +
`gb_Deliver_deliver: ... libsal_textenclo.dylib ...`)。
BUILDDIR `workdir` 841M 但 fresh state、`instdir` 几乎空。

→ **影响**: D3.1 / D3.3 落地后无法在本机跑 svidl + 全量编译验证。
依赖未来有 build infra 的 session 补 verify。

### 🟢 B3 — 远端推送被禁

F2 决策: 无 SSH key + HTTPS 2GB pack 触 500MB 限制。
SRCDIR ahead origin **18** commits，全部本地。

### ⚪ B4 — 上层授权未到

| # | 待授权 | 影响 |
|---|---|---|
| D1 | W3 Day-1b SwDocShell wiring (`sw/source/uibase/app/docsh*.cxx`) | 关闭 W3 最后一道 Day-1 关 |
| D3 | W2 Day-1b popover GUI shell (`cui/source/dialogs/commandpalette/` hosting) | 关闭 W2 Day-1b 实际使用链 |
| D5 | `downstream-branding/` source of truth | CI 静默 build without branding |
| W4 scope | `sw/source/uibase/inline-actions/` et al. | 开 W4 Day-0 |
| W5 scope | `kqoffice/source/ai/cowork/**` | 开 W5 Day-0 C++（schema 已落） |

D2 (W1 Day-1a)、D4 (I1)、D6 (worker scope) 均已 RESOLVED / PARTIALLY
RESOLVED，详见 `lane-status.md §"Open decisions"`。

---

## 7. 三条铁律 (本会话约束)

1. **本地优先 (Ollama)**
2. **不静默上传**
3. **AI 输出对象级 patch**

合规授权清单 (clavue.md): `sfx2 sdi` / `officecfg` / `cui commandpalette` /
`sw docsh`。其他模块改动需重新授权。

---

## 8. V2 Contract Harnesses

4 个 V2 contract harness，全部必须在 `main` 分支上保持绿色。CI 走
`.github/workflows/v2-contract-harnesses.yml`，~5 分钟无外部依赖。

| Harness | Locks | 当前状态 |
|---|---|---|
| `tests/v2-day0-skeleton-test.sh` | Day-0 skeleton doc structure | passed, 26 checks |
| `tests/v2-plan-baseline-test.sh` | Spec docs + 28 fixtures / 12 schemas + goals.json completed + W4/W5 Day-0 plan + enum-lock 表 + ledger 计数 | passed, 26 checks |
| `tests/v2-provider-evidence-schema-test.sh` | Schema ↔ C++ token parity (3-way) | passed |
| `tests/v2-async-task-schema-test.sh` | W5 async-task schema enum order + fixture validity（partial-enforce；auto-promotes to full-enforce once `AsyncTask.hxx` lands） | passed (partial) |
| `bin/intelligent-contract-fixtures.sh` | V1.5 m3-02 + V2 fixture roster (28/28) | passed |

### 8.1 Drift locks (programmatic, last green 2026-05-10)

| Lock | 验证脚本 |
|---|---|
| Schema status enum ↔ C++ apply-plan-* 13/13 | `tests/v2-provider-evidence-schema-test.sh` |
| 防御性 `apply-plan-unknown` 在 C++、不在 schema | 同 |
| `Provider.cxx` runtime token ⊆ schema runtime enum | 同 |
| 9-key `EvidenceRecorder` envelope shape | 同 |
| `ApplyPlanValidator.hxx` 14-tag enum | `testApplyPlanCodeEnumStable` |
| `applyPlanValidationStatus` distinct-per-code | `testApplyPlanStatusDistinctPerCode` |
| `applyPlanValidationMessage` distinct-per-code | `testApplyPlanMessageDistinctPerCode` |
| W5 async-task schema enum order = token-lock table order | `tests/v2-async-task-schema-test.sh` (partial-enforce) |
| W4 + W5 specs carry `## Day-0 Entry-Point Plan` | `tests/v2-plan-baseline-test.sh` check 7 |
| W4 `### Action enum lock` / W5 `### Token lock` 子节 | `tests/v2-plan-baseline-test.sh` check 8 |
| lane-status.md ledger 计数 = `wc -l` ledger.jsonl | `tests/v2-plan-baseline-test.sh` check 9 |
| V1.5 27/27 strict roundtrip | `bin/intelligent-contract-fixtures.sh` |

`tests/v2-plan-baseline-test.sh` 当前 baseline:
**Fixtures 28 across 12 schemas / Checks 26 / passed**（2026-05-10 L41 后实跑）。

---

## 9. 接下来可动 (无授权 reversible)

按优先级：

1. **`async-task.terminal-failed.json` 边界 fixture** — extended-naming
   形态，`state: failed` + `failure_reason` 必填的覆盖；baseline 28→29。
2. **`docs/schemas/async-task.schema.md` schema 人工解读** — 14-prop
   schema 有自描述但读者手册缺，可独立成文。
3. **W3 validator 多一路边界 fixture** — `apply-plan-runtime.utf8.json`
   等，覆盖多字节/零宽字符 round-trip；H3 无需调。

需授权才能动（B4 未到）：W2 D3.4 cppunit + UITest、W3 Day-1b、
W4 Day-0、W5 Day-0 C++。

---

## 10. 文档地图

权威性顺序（V2 改动前必读）:

1. `docs/CLAUDE-NOTES.md` — durable guidance（不会被 /init 覆写）
2. `docs/product/v2/lane-status.md` — 实时 wave 状态
3. `.agent/goals/2026-05-08-v2-ai-native/ledger.jsonl` — append-only 时间线 (**41** 行)
4. `docs/product/v2/day1-progress.md` — Day-1{a..h} per-step rationale
5. `docs/product/v2/w[1-5]-*-spec.md` — wave 设计 spec（W4/W5 含 Day-0 plan + enum/token lock）
6. `docs/product/v2-master-plan.md` — V2 总图

辅助:
- `docs/schemas/*.schema.json` — 12 schemas (provider-request /
  provider-evidence / apply-plan-runtime / async-task / evidence-record /
  apply-plan / document-snapshot / intelligent-diagnostic /
  kqoffice-plugin / presentation-outline / preview-action /
  capability-registry-entry)
- `docs/schemas/fixtures/` — 28 fixtures
- `docs/v2-commit-manifest-2026-05-10.md` — V2 commit 清单
- `docs/v2-coordinator-handoff-2026-05-10.md` — 8-worker sweep 交接

---

## 11. Memory 索引 (auto-memory)

`/Users/lu/.clavue/projects/-Users-lu---office/memory/MEMORY.md`:

- 世界级质量目标
- ICC peer terminal 提交验证
- Build artifacts in git index (永不 `git add .`)
- V2 AI-native entry pointer (改动前阅读顺序)
- V2 hard invariants (Schema↔C++ token parity / 防御 unknown / Provider.cxx subset / V1.5 baseline / 命名 — harness 锁定)
- /goal mode coordinator churn detection

---

## 12. 本会话（2026-05-10）增量

10 个 ledger 行（L32-L41）、BUILDDIR 10 个 commit（`a3a0fe0` 起至
HEAD `1792e0a`）、SRCDIR 1 个 commit（`8fe469f71`）。主题：

- L32-L33：W1 D2 audit、D1/D3 source-tree audit + commit decision close-out
- L34-L35：W2 S4 close-out + W4/W5 Day-0 entry plans
- L36：D6 worker-scope mapping PARTIAL resolution
- L37：W4 + W5 spec enum/token lock 表落定（修正 L35 Day-0 plan 中的旧 token 漂移）
- L38：lane-status 镜像 catch-up（L37 漏镜像）+ ledger 计数刷 20→37→38
- L39：`tests/v2-async-task-schema-test.sh` skeleton harness（schema 未到前 skip exit 0）
- L40：`tests/v2-plan-baseline-test.sh` 加 5 个结构锁（W4/W5 Day-0 章节、W4 enum lock、W5 token lock、ledger 计数镜像）21→26 checks
- L41：`async-task.schema.json` + 2 fixtures 落、H4 升级到 partial-enforce、H2 baseline 26/11→28/12

4 个 harness 状态：H1 ✓ / H2 ✓ (28/12) / H3 ✓ / H4 partial ✓；
ai-native cppunit 77→84（W2 controller +7）；Mao final gate=passed。

---

**状态梳理完成。B1 拍板后（或 W3 Day-1b / W4 / W5 C++ 授权到位后）立即续推。**
