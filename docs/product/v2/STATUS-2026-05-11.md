# 可圈office V2 当前状态总览

> **生成时间**: 2026-05-11（L46 落定后刷新）
> **生成者**: Clavue (Opus 4)
> **数据源**: `lane-status.md` / `ledger.jsonl` / `git log` / `goals.json` / 实时仓库状态
>
> 这是一份**状态快照**（read-only），不包含新决策。决策入口仍在
> `lane-status.md §"Open decisions for supervisor"`。
>
> 上一版：`STATUS-2026-05-10.md`（L41/L42 close-out）

---

## 1. 一句话总览

V1.5 已完工并打 milestone；V2 (AI-native office) 在两个会话内完成了
**W2 S4 controller** 落地、**W3 utf8 边界 fixture**、**W5 schema +
fixtures + 终态 fixture + reader's manual** 全套 documentation-tier
+ harness、**W4 schema + fixtures + H5 partial-enforce** 全套。
当前阻塞点：**W2 D3.3b Cmd+K 冲突**（B1，A/B 待拍）、**W3 Day-1b** /
**W4 Day-0** / **W5 Day-0 C++** 等授权（D1/Scope）、**B2** 本地无法
编译、**F2** 推送被禁。**5 个 V2 contract harness 全绿**。

---

## 2. 仓库形态

```
BUILDDIR  /Users/lu/可点office          # 配置过的 LibreOffice 风格构建树
SRCDIR    /Users/lu/kdoffice-src        # 源码 git checkout
INSTDIR   instdir/可圈office.app        # macOS app bundle 输出位
WORKDIR   workdir/                      # gbuild 中间产物
```

- BUILDDIR 顶层 `Makefile` 是 autogen 生成的，模块快捷键如
  `make sw.build` / `make sc.build` 走 `solenv/gbuild/partial_build.mk`。
- `instdir/` 现状: `Contents/` 存在但近乎空 → 没有可运行的 .app。
- `workdir/` 有目录结构 (CppunitTest/CxxObject/...) 但是 fresh state，
  非全量构建产物。

---

## 3. 工具链

| 项 | 值 |
|---|---|
| 平台 | macOS Darwin 24.6.0 (arm64) |
| Xcode | 26.3 |
| macOS SDK | 26.2 |
| min target | 11.0 |
| GNU Make | 4.4.1 (`/opt/homebrew/bin/gmake`) |
| pkg-config | pkgconf 2.5.1 (`/opt/homebrew/bin/pkg-config`) |
| 模型 | Clavue Opus 4 (`claude-opus-4-7`) |
| clang | Apple clang 17.0.0 |

---

## 4. 当前 git 状态

### BUILDDIR (`/Users/lu/可点office`) — branch `main`

```
HEAD  920a78a  schemas: V2 W4 inline-action-request schema + 4 fixtures + H5 partial-enforce (L46)
       80775d7  docs: async-task schema reader's manual (L45)
       917718f  fixtures: V2 W3 apply-plan-runtime UTF-8 multi-codepoint boundary fixture (L44)
       59daa6c  fixtures: V2 W5 async-task terminal-failed boundary fixture (L43)
       c03e932  docs: STATUS-2026-05-10 full refresh to L41 close-out (L42)
       1792e0a  schemas: V2 W5 async-task schema + fixtures + H4 partial-enforce (L41)
       94eaee6  tests: H2 grows 21 → 26 checks; structural V2 plan locks (L40)
       a940134  tests: v2-async-task-schema-test.sh skeleton harness for W5 Day-0 (L39)
       12381f3  docs: lane-status mirror catch-up for L37 + entry count (L38)
       782a618  docs: V2 W4/W5 spec gain explicit enum-lock tables (L37)
```

dirty: 几乎全是 `instdir/` / `workdir/` / `autom4te.cache/` 生成物 —
不会被 commit（按 memory 规则永远只用显式路径 add）。
`sysui/desktop/macosx/LaunchConstraint.plist` team-identifier 被清空
未确认是否有意（B2 后续复核项）。

### SRCDIR (`/Users/lu/kdoffice-src`) — branch `main`, ahead origin **18**

```
HEAD  8fe469f71  feat(cui): inline CommandPaletteController + add controller cppunit (V2 W2 S4)
       de0fd779f  feat(sfx2): register SID_COMMAND_PALETTE slot for V2 W2 Cmd+K     # D3.1
       21ecc7966  .gitignore: ignore local /.claude /.clavue agent metadata
       1ad4e428f  V1.5 i18n tail: include/sfx2 strings + openoffice.lst + reportdesign menubar
       ...
```

**未推远端** (F2 决策: 无 SSH key + HTTPS 2GB pack 限制 500MB)。

未 commit 改动: `officecfg/.../GenericCommands.xcu` +11 行 →
`.uno:CommandPalette` label node (D3.3a)。仍在 SRCDIR 待 B1 拍板后
并入单个 accelerator+label commit。

---

## 5. 路线图与各 Wave 状态

### V1.5 ✅ 已完工 (milestone `2026-05-08-v1.5-completion`)

- zh-CN i18n 全套 / 商务模板 + 可圈office 品牌资产 / macOS web/template
  iconset 刷新 / workbench UITest smoke 套件 / IconHelper / tray /
  sidebar 视觉精修 / 27/27 strict roundtrip baseline 锁定。

### V2 (`2026-05-08-v2-ai-native`) — AI-native office

| Wave | 焦点 | 状态 |
|---|---|---|
| W1 | Provider runtime (Ollama-first 沙箱) | 主体已落地 + 51 cppunit |
| W2 | Cmd+K 命令调色板 | Controller cppunit 落地（S4）；**D3.3b 卡 Cmd+K 冲突等 B1 拍板** |
| W3 | Writer apply-runtime + ApplyPlan validator | Day-1{a,c-h} ✅, **Day-1b ⛔ 待 D1**; utf8 multi-codepoint fixture L44 |
| W4 | Select-to-act 浮窗 / Diff 审批 | spec + Day-0 plan + enum lock (L37) + **schema + 4 fixtures + H5 partial-enforce (L46)** |
| W5 | Async cowork (长任务 + diff review) | spec + Day-0 plan + token lock (L37) + **schema + fixtures + 终态 fixture + reader's manual + H4 partial-enforce (L41+L43+L45)** |

**ai-native cppunit 总数**: **84** cases
（51 provider + 33 cui = 8 idx + 8 fuzzy + 10 recent + 7 controller）。

### W3 — 详情 (utf8 fixture L44)

| Step | 状态 | Notes |
|---|---|---|
| Day-1a | ✅ | `ApplyPlanValidator.hxx` header-only, 14/14 ValidationCode |
| Day-1b | ⛔ | `SwDocShell::applyDiagnosticsPlan` wiring（待 **D1**） |
| Day-1c | ✅ | `applyPlanValidationMessage` 14 zh-CN toast |
| Day-1d | ✅ | parser primitives micro-tests +5 |
| Day-1e | ✅ | `applyPlanValidationStatus` 14-token ASCII kebab +3 |
| Day-1f | ✅ | `provider-evidence.schema.json` + 3 fixtures |
| Day-1g | ✅ | UTF-8 multi-codepoint cppunit +5；schema-level fixture `apply-plan-runtime.utf8.json` (L44) 覆盖 ZWJ 家族 emoji + RTL 混排 + CJK ext-B + 嵌套转义 |
| Day-1h | ✅ | `tests/v2-provider-evidence-schema-test.sh` schema↔C++ drift lock |

### W4 — 详情 (schema + H5 L46)

Spec + Day-0 Entry-Point Plan + **Action enum lock** (L37) +
**schema + 4 fixtures + H5 partial-enforce** (L46)。

`docs/schemas/inline-action-request.schema.json`:
- 7 top-level required keys + `additionalProperties:false` + 3-branch
  `oneOf` keyed on `surface` const
- writer-paragraph 分支锁 ParagraphAction 7 token
- calc-cell 分支锁 CellAction 5 token
- impress-slide-element 分支锁 SlideElementAction 4 token

4 个 fixture: `valid` (writer rewrite minimal) / `calc-suggest-chart`
(extended-naming) / `impress-translate` (extended-naming) /
`invalid` (9 violations)。

H5 (`tests/v2-inline-action-request-schema-test.sh`) partial-enforce
模式 — schema oneOf 3 branch 锁 + 每分支 action enum 顺序锁 + 4
fixture 验证；当
`${KDOFFICE_SRC_ROOT}/sw|sc|sd/source/.../inline-actions/*.hxx` 全部
落地时自动升级到 full-enforce。

C++ 仍 gated（`sw/source/uibase/inline-actions/` /
`sc/source/ui/inline-actions/` / `sd/source/ui/inline-actions/` /
`svx/source/sidebar/diff-review/`）。

### W5 — 详情 (full doc-tier landed L41+L43+L45)

Spec + Day-0 Entry-Point Plan + **Token lock** (L37) + **schema**
(L41) + **valid + invalid + terminal-failed fixtures** (L41+L43) +
**reader's manual** (`async-task.schema.md` L45) +
**H4 partial-enforce** 模式。

3 个 fixture 覆盖矩阵:
- `async-task.valid.json` — awaiting-review happy path
- `async-task.invalid.json` — 11 distinct violations
- `async-task.terminal-failed.json` — `state=failed` + `failure_reason`
  + `result_plan_id=null` 边界

C++ 仍 gated（`kqoffice/source/ai/cowork/**` +
`kqoffice/qa/cppunit/test_cowork*`）。

---

## 6. 当前阻塞 (ordered by 紧急度)

### 🔴 B1 — Cmd+K 快捷键冲突

`Accelerators.xcu:93` `K_MOD1` 已绑定 `.uno:HyperlinkDialog`。

**待用户拍板 A/B**:
- **A**: 抢占 K_MOD1 → CommandPalette。代价: HyperlinkDialog 失去 Cmd+K（Word/Pages/Google Docs 全行业 Cmd+K=Hyperlink）。
- **B** (推荐): CommandPalette → MOD1_SHIFT+K (Cmd+Shift+K)。代价: 偏离 spec，但零回归。

### 🟡 B2 — 无法本地编译验证

`sfx2.build` 失败 (`concat-deps: No such file or directory` +
`gb_Deliver_deliver: ... libsal_textenclo.dylib ...`)。
依赖未来有 build infra 的 session 补 verify。

### 🟢 B3 — 远端推送被禁

F2 决策: 无 SSH key + HTTPS 2GB pack 触 500MB 限制。
SRCDIR ahead origin **18** commits，全部本地。

### ⚪ B4 — 上层授权未到

| # | 待授权 | 影响 |
|---|---|---|
| D1 | W3 Day-1b SwDocShell wiring (`sw/source/uibase/app/docsh*.cxx`) | 关闭 W3 最后一道 Day-1 关 |
| D3 | W2 Day-1b popover GUI shell (`cui/source/dialogs/commandpalette/` hosting) | 关闭 W2 Day-1b 实际使用链 |
| D5 | `downstream-branding/` source of truth | CI 静默 build without branding |
| W4 scope | `sw/source/uibase/inline-actions/` et al. | 开 W4 Day-0 C++（schema + H5 已落，C++ 一接就走） |
| W5 scope | `kqoffice/source/ai/cowork/**` | 开 W5 Day-0 C++（schema + reader's manual 已落） |

D2 (W1 Day-1a)、D4 (I1)、D6 (worker scope) 均已 RESOLVED / PARTIALLY
RESOLVED，详见 `lane-status.md §"Open decisions"`。

---

## 7. 三条铁律 (本会话约束)

1. **本地优先 (Ollama)**
2. **不静默上传**
3. **AI 输出对象级 patch**

合规授权清单 (clavue.md): `sfx2 sdi` / `officecfg` / `cui commandpalette` /
`sw docsh`。其他模块改动需重新授权。

---

## 8. V2 Contract Harnesses

**5 个** V2 contract harness（W4 schema 落地后 H4 → H4+H5），全部必须
在 `main` 分支上保持绿色。CI 走 `.github/workflows/v2-contract-harnesses.yml`，
~5 分钟无外部依赖。

| Harness | Locks | 当前状态 |
|---|---|---|
| `tests/v2-day0-skeleton-test.sh` | Day-0 skeleton doc structure | passed, 26 checks |
| `tests/v2-plan-baseline-test.sh` | Spec docs + 34 fixtures / 13 schemas + goals.json completed + W4/W5 Day-0 plan + enum-lock 表 + ledger 计数 | passed, 26 checks |
| `tests/v2-provider-evidence-schema-test.sh` | Schema ↔ C++ token parity (3-way) | passed |
| `tests/v2-async-task-schema-test.sh` (H4) | W5 async-task schema enum order + fixture validity（partial-enforce） | passed (partial) |
| `tests/v2-inline-action-request-schema-test.sh` (H5) | W4 inline-action-request schema 3-branch oneOf action enum order + fixture validity（partial-enforce） | passed (partial) |
| `bin/intelligent-contract-fixtures.sh` | V1.5 m3-02 + V2 fixture roster (34/34) | passed |

### 8.1 Drift locks (programmatic, last green 2026-05-11)

| Lock | 验证脚本 |
|---|---|
| Schema status enum ↔ C++ apply-plan-* 13/13 | `tests/v2-provider-evidence-schema-test.sh` |
| 防御性 `apply-plan-unknown` 在 C++、不在 schema | 同 |
| `Provider.cxx` runtime token ⊆ schema runtime enum | 同 |
| 9-key `EvidenceRecorder` envelope shape | 同 |
| `ApplyPlanValidator.hxx` 14-tag enum | `testApplyPlanCodeEnumStable` |
| `applyPlanValidationStatus` distinct-per-code | `testApplyPlanStatusDistinctPerCode` |
| `applyPlanValidationMessage` distinct-per-code | `testApplyPlanMessageDistinctPerCode` |
| W5 async-task schema enum order = token-lock table order | `tests/v2-async-task-schema-test.sh` (partial) |
| **W4 inline-action-request schema oneOf 3-branch action enum order = W4 spec §"Action enum lock" 表 (L46)** | `tests/v2-inline-action-request-schema-test.sh` (partial) |
| W4 + W5 specs carry `## Day-0 Entry-Point Plan` | `tests/v2-plan-baseline-test.sh` check 7 |
| W4 `### Action enum lock` / W5 `### Token lock` 子节 | `tests/v2-plan-baseline-test.sh` check 8 |
| lane-status.md ledger 计数 = `wc -l` ledger.jsonl | `tests/v2-plan-baseline-test.sh` check 9 |
| V1.5 27/27 strict roundtrip | `bin/intelligent-contract-fixtures.sh` |

`tests/v2-plan-baseline-test.sh` 当前 baseline:
**Fixtures 34 across 13 schemas / Checks 26 / passed**（2026-05-11 L46
后实跑）。

---

## 9. 接下来可动 (无授权 reversible)

按优先级：

1. **`async-task.cancelled.json` 边界 fixture** — `state=cancelled`
   + 用户中断 + 无 `failure_reason` 必填的覆盖；baseline 34→35。
2. **`inline-action-request.writer-custom.json`** — `action=custom`
   + `user_prompt` 必填示范；baseline +1。
3. **W5 spec §"Reader's manual" 链接段** — 把 schema doc 引用从
   lane-status 升级到 spec 内部，让 onboarding-into-W5 一站式。
4. **`docs/schemas/inline-action-request.schema.md`** — W4 schema
   reader's manual（参照 W5 doc 体例）。

需授权才能动（B4 未到）：W2 D3.4 cppunit + UITest、W3 Day-1b、
W4 Day-0、W5 Day-0 C++。

---

## 10. 文档地图

权威性顺序（V2 改动前必读）:

1. `docs/CLAUDE-NOTES.md` — durable guidance（不会被 /init 覆写）
2. `docs/product/v2/lane-status.md` — 实时 wave 状态
3. `.agent/goals/2026-05-08-v2-ai-native/ledger.jsonl` — append-only 时间线 (**46** 行)
4. `docs/product/v2/day1-progress.md` — Day-1{a..h} per-step rationale
5. `docs/product/v2/w[1-5]-*-spec.md` — wave 设计 spec（W4/W5 含 Day-0 plan + enum/token lock）
6. `docs/product/v2-master-plan.md` — V2 总图
7. `docs/schemas/async-task.schema.md` — W5 reader's manual

辅助:
- `docs/schemas/*.schema.json` — **13** schemas (provider-request /
  provider-evidence / apply-plan-runtime / async-task /
  **inline-action-request** / evidence-record / apply-plan /
  document-snapshot / intelligent-diagnostic / kqoffice-plugin /
  presentation-outline / preview-action / capability-registry-entry)
- `docs/schemas/fixtures/` — **34** fixtures
- `docs/v2-commit-manifest-2026-05-10.md` — V2 commit 清单
- `docs/v2-coordinator-handoff-2026-05-10.md` — 8-worker sweep 交接

---

## 11. Memory 索引 (auto-memory)

`/Users/lu/.clavue/projects/-Users-lu---office/memory/MEMORY.md`:

- 世界级质量目标 / ICC peer terminal 提交验证 / Build artifacts in
  git index (永不 `git add .`) / V2 AI-native entry pointer / V2 hard
  invariants / /goal mode coordinator churn detection。

---

## 12. 本会话（2026-05-10 → 2026-05-11）增量

L42-L46 五条 ledger，5 个 BUILDDIR commit:

- L42: STATUS-2026-05-10 full refresh to L41 close-out (`c03e932`)
- L43: W5 async-task `terminal-failed` boundary fixture (`59daa6c`)
- L44: W3 apply-plan-runtime `utf8` multi-codepoint boundary fixture (`917718f`)
- L45: async-task schema reader's manual (202 行) (`80775d7`)
- L46: **W4 inline-action-request schema + 4 fixtures + H5 partial-enforce** (`920a78a`)

5 个 harness 状态: H1 ✓ / H2 ✓ (34/13) / H3 ✓ / H4 partial ✓ / H5
partial ✓；ai-native cppunit suite 84 不变；V1.5 27/27 baseline 不变；
Mao final gate=passed。

---

**状态梳理完成。B1 拍板后或 W3 Day-1b / W4 / W5 C++ 授权到位后立即续推；
schema + harness 已为各 wave 准备好 enum-stability 落地的最短路径。**

---

## 13. 晚间增量 (2026-05-11 evening, L47→L78)

> 本节为 morning snapshot (L46 close-out) 的 append-only 续写。第
> 1–12 节是 L46 时点冻结视图，下面是同日 evening 截至 L78 的增量；
> 如果两节冲突，**evening 节为准**（生成时间更晚）。

### 13.1 一句话变化

5 harness → **7 harness**（H6 reader's-manual coherence + H7 apply-plan-runtime），
H2 baseline 26 checks → **45**（新增 7-15 共 9 个 lock）；ledger 46 → **78 行**；
fixtures 34 → **36/13**；OK(84) cppunit claimed 不变但**workdir verify 77** +
B2 阻塞 upgrade-path（L75 honest split）。无新 wave landed，
全是 documentation/locking/honesty-correction tier 工作面。

### 13.2 新增 harness (相对 §6)

| Harness | Lock | 落定 |
|---|---|---|
| H6 `v2-schema-manual-coherence-test.sh` | 4 reader's manual fact-block ↔ schema body bidirectional | L58/L59（glob-discovered + 5-key allow-list）|
| H7 `v2-apply-plan-runtime-schema-test.sh` | W3 schema kind enum order ↔ spec table + envelope guards (**partial**) | L55 |

`bin/v2-harness-sweep.sh` (L65) 是 7-harness 单次入口；`--with-fixtures`
追加 V1.5+V2 fixture 跑 + ≥36/0 断言。CI workflow paths-filter (L73)
覆盖 sweep 脚本 + ledger 目录。

### 13.3 H2 新增 check (7→15, 相对 §6)

| # | Lock | 落定 | Drift class |
|---|---|---|---|
| 7 | W4 + W5 specs Day-0 Entry-Point Plan 段 | pre-L62 | Day-0 spec 完整性 |
| 8 | W4 + W5 specs enum-lock 子节 | pre-L62 | enum 漂移 |
| 9 | lane-status.md `(N entries)` = `wc -l` ledger | pre-L62 | ledger 计数撒谎 |
| 10 | reader's manual ↔ lane-status bidirectional | L58 | 文件存在但未引 / 引用但无文件 |
| 11 | lane-status 引 `tests/v2-*.sh` 路径存在 + 0755 | L60 | 重命名/掉权限 |
| 12 | ledger 行 canonical 4-key shape | L66 | L65 事件：drop `event` key |
| 13 | pass-count baseline 3-site 镜像 (CLAUDE-NOTES + handoff + sweep header) | L67 | 单点 bump 漏其他 |
| 14 | `bin/v2-harness-sweep.sh` exec-bit | L70 | sweep silently fails permission |
| 15 | sweep enumeration ↔ disk parity (双向) | L76 | 新 harness 不进 sweep / sweep 引不存在的 |

每个 check 12-15 都有 inline negative-canary（CLAUDE-NOTES
§"Verifying check 12/13/14/15 actually catch drift"，L69 起 +
L77 扩展），touch 时跑过证明 lock 真抓 drift class。

### 13.4 4-tier locking architecture (post-L67/L76)

1. Schema body ↔ C++ tokens (H1 full / H4/H5/H7 partial)
2. Schema body ↔ reader's manual fact-block (H6, glob-discovered)
3. lane-status ↔ on-disk artifacts (H2 9/10/11)
4. **Self-consistency of locking system itself** (H2 12/13/14/15) ←
   tier 4 born from L65 silently 漂的事件

### 13.5 Honesty corrections (L72, L75)

- **L72**: OK(84) cppunit total 是 51 provider + 33 cui = 8 idx + 8
  fuzzy + 10 recent + **7 controller**。Workdir 仅 verify 77 (前 4
  binaries)；7-case `cui_commandpalette_controller` 在 SRCDIR
  (`cui/qa/unit/CommandPaletteControllerTest.cxx`) 但 BUILDDIR
  workdir 无 `.test.log` → OK(84) 是 **claimed**, 不是 verified。
- **L75**: 尝试 `make CppunitTest_cui_commandpalette_controller`
  升级 77→84 失败 — harfbuzz meson `UnicodeDecodeError byte 0xe5
  position 13` 因 BUILDDIR 根含非 ASCII `可点office`。这是 **B2-class**
  build-env 阻塞，不是 wall-clock。Log: `/tmp/cppunit-controller-build.log`
  (1561 行)。所以 OK(84) upgrade-path = "rename BUILDDIR ASCII-only OR
  patch meson `LC_ALL=C`"，不是 "wait for budget"。

### 13.6 新增 handoff 阻塞 (D8/D9, L71)

| ID | 决策 | Blocks |
|---|---|---|
| D8 | plist `team-identifier`（working tree 空，main `2B7JZ4N26U`）| macOS codesigning |
| D9 | commit SRCDIR `officecfg/.../GenericCommands.xcu` `.uno:CommandPalette` 11-行 en-US label（uncommitted in `/Users/lu/kdoffice-src` HEAD `8fe469f71`）| W2 D3 最小 sub-step |

### 13.7 接下来可动

**Reversible 工作面真饱和**。L65-L78 共 14 commit 后所有 4 tier
都铺到位 + canary 协议覆盖全部 self-consistency check + 4 个文档系统
(handoff + lane-status + CLAUDE-NOTES + 6 个 off-repo memory 文件)
全部同步到 L78。

继续需要 user-only：

- **授权门**: D1 / D3 / D5 / D6 / D8 / D9 / W4 scope / W5 scope
- **环境**: B1 (Cmd+K accelerator 冲突，gated on D3) / B2 (BUILDDIR
  非 ASCII path / LC_ALL=C) / B3 (无 SSH push)

### 13.8 文档地图 delta (相对 §10)

L46 后新增：

- `docs/v2-coordinator-handoff-2026-05-10.md`（L64 起，**L77 refresh**）
  — wait-state snapshot, 必读
- `docs/schemas/{provider-evidence,apply-plan-runtime,inline-action-request}.schema.md`
  — 4 reader's manuals 共计 (含 async-task.schema.md from L45)
- `bin/v2-harness-sweep.sh` (L65) — 7-harness 单次入口
- 6 off-repo memory 文件 (L63 seed + 多次 refresh) 在
  `/Users/lu/.clavue/projects/-Users-lu---office-clmpx2/memory/`

### 13.9 本会话 evening commits (L65-L78, 14 个)

L65 sweep 脚本 / L66 ledger row-shape lock / L67 baseline 3-site lock /
L68 lock-system semantics docs / L69 canary 协议 / L70 sweep exec-bit /
L71 D8/D9 blockers / L72 OK(84) claimed-vs-verified split /
L73 CI trigger cover sweep+ledger / L74 handoff refresh L67→L73 /
L75 cppunit B2-blocked honesty / L76 sweep enumeration parity (H2 →45) /
L77 canary 扩展 cover 14+15 / **L78 handoff refresh L73→L77 + memory
locking-architecture sync**

H1=26 / H2=**45** / H3=26 / H4 partial / H5 partial / H6=39 /
H7 partial。fixtures 36/0/13。ai-native cppunit OK(84) claimed,
77 workdir-verified（controller 7 B2-blocked）。V1.5 27/27 不变。

## 14. 深夜增量 (2026-05-12 late, L79→L83)

> §13 是 L78 frozen-snapshot；本节 append §13 之后 L79→L83 同一 /goal
> session 的 5 个 commit。**conflict 时本节为准**（生成时间最晚）。

### 14.1 一句话

L79-L83 共 5 commit，新增 H2 check 16 (ledger ts ISO-8601 + UTC
monotonicity)；H2 baseline 45 → 46；4-tier locking 架构 tier-4 扩展
checks 12/13/14/15 → 12/13/14/15/16；handoff doc + STATUS 自身均按
"refresh-every-~4-commits" 节奏 catch up。

### 14.2 5 个 commit（L79-L83）

| L | commit | 摘要 |
|---|---|---|
| L79 | `16b60d3` | STATUS-2026-05-11 §13 L47-L78 evening delta（本文档前节）|
| L80 | `80ccd9a` | v2-commit-manifest superseded-banner + off-repo memory refresh |
| L81 | `fb70067` | CLAUDE-NOTES stale L-pointer refresh L63→L77 + post-L67→post-L76（in-repo 跨文 L-anchor 漂移修正，H2 check 13 不锁 L-number）|
| L82 | `6992246` | handoff refresh L77→L81 + 4-commit-cadence pattern 标记 |
| L83 | `14a033e` | **H2 check 16 ledger ts ISO-8601 + UTC monotonicity**（新 tier-4 lock，H2 45→46）|

### 14.3 H2 check 16 (新 tier-4 lock, post-L83)

L82 余波审计发现：jq + 朴素 shell string-compare 在 ledger row 5 误报
NON-MONOTONIC，因为 row 1-4 用 `+0800`、row 5-82 用 `+0000`，原始字符串
比较把 `+0800 > +0000`。实际 UTC instants 全 82 行非递减（python
datetime 验证）。check 16 enforce 三件事:

1. ts 必须正则匹配 `[+-][0-9]{4}$`（**禁** 裸 `Z` 后缀和无 tz 格式）
2. ts 必须能被 BSD `date -j -f "%Y-%m-%dT%H:%M:%S%z"` 解析为 epoch
3. epoch 必须 ≥ 前一行 epoch（UTC-normalized 非递减）

两个 negative canary 现场跑过：(a) 把最后一行 ts 改成 `2020-01-01...`
触发 backward-UTC FAIL；(b) 改成 `Z` 后缀触发 missing-numeric-tz FAIL。
canary 协议块加进 CLAUDE-NOTES §"Verifying check 12/13/14/15/16
actually catch drift"。

### 14.4 4-tier locking architecture (post-L83)

| Tier | 检查 | 范围 | 自动伸展 |
|---|---|---|---|
| 1 schema↔C++ | H1+H4/5/7 | apply-plan/runtime/async-task/inline-action 状态字 | C++ header 落地后 partial→full 自动促进 |
| 2 schema↔manual | H6 | 4 reader's manuals fact-block ↔ schema body | glob-discovered，新 `*.schema.md` 自动入册 |
| 3 lane-status↔artifacts | H2 9/10/11 | ledger-count, manual roster, harness path+0755 | 双向；任一漂移 fail CI |
| **4 self-consistency** | **H2 12/13/14/15/16** | row-shape, baseline 3-site, sweep exec-bit, sweep↔disk parity, **ledger ts ISO+UTC monotonicity** | 锁住 locking 体系自身；L65-L83 一系列事件迭代而成 |

tier-4 在本日累计添加 5 个 self-consistency lock，全部源于 L65 起的
具体 drift 事件：L65 row 多键 → L66 check 12 / L67 baseline 镜像漂移
→ L67 check 13 / L70 sweep 失 0755 → L70 check 14 / L76 新 harness
未入 sweep → L76 check 15 / **L82 ledger ts mixed-tz 字符串比较误报 →
L83 check 16**。每个事件都有具体复现路径 + 现场 negative canary。

### 14.5 L83 后基线

```
H1=26 / H2=46 / H3=26 / H4 partial / H5 partial / H6=39 / H7 partial
fixtures 36/0/13；V1.5 27/27 不变；ai-native cppunit OK(84) claimed /
OK(77) workdir-verified（B2 blocks controller 7-case 升级）
ledger 83 行
```

3 处镜像（H2 check 13 enforce）已对齐 H2=46：CLAUDE-NOTES（canonical）
+ handoff doc + bin/v2-harness-sweep.sh header。Off-repo 镜像
`v2_entry_pointer.md` 待下一次 memory refresh batch 同步（不强制）。

### 14.6 仍开放（与 §13.5 一致）

D1/D3/D5/D6/D8/D9 + W4/W5 scope auth：全部 user-only。
B1（Cmd+K 冲突 D3 gated）/ B2（BUILDDIR 非 ASCII / harfbuzz meson
UnicodeDecodeError）/ B3（push gated 无 SSH key + 500MB pack 限制）。

## 15. 凌晨增量 (2026-05-12 dawn, L84→L86)

> §14 是 L83 frozen-snapshot；本节 append §14 之后 L84→L86 同一 /goal
> session 的 3 个 doc-refresh commit。**conflict 时本节为准**（生成
> 时间最晚）。

### 15.1 一句话

L84-L86 共 3 commit，全部 doc/anchor refresh，无 harness/baseline 变化。
"~4-commit handoff staleness" cadence pattern 在 L85 自我确认（第 4 次
复现），同时 CLAUDE-NOTES anchor drift cadence 实测比 handoff cadence
快（2 commits in 即漂）。

### 15.2 3 个 commit（L84-L86）

| L | commit | 摘要 |
|---|---|---|
| L84 | `72e2c06` | STATUS-2026-05-11 §14 late-night L79-L83 delta（本文档上节）|
| L85 | `b4e4c5f` | handoff enumeration extend L81 → L84 = cadence-pattern 4th recurrence（L74/L78/L82/L85 节奏自我确认）|
| L86 | `8ca35b5` | CLAUDE-NOTES line 9 handoff-anchor refresh L77 → L85（L81 已识别同 drift class 复现，但 cadence 比 handoff 更快）|

### 15.3 Cadence pattern 量化总结 (post-L86)

L82 commit message 自我预言 "Cadence pattern emerging: handoff refresh
every ~4 commits when running /goal autonomous reversible track"。截至
L86 经验证:

| Drift class | 复现间隔 | 实例 |
|---|---|---|
| handoff title/state/enumeration | ~4 commits | L74 (L67→L73) / L78 (L73→L77) / L82 (L77→L81) / L85 (L81→L84) |
| CLAUDE-NOTES line-9 cross-doc anchor | ~2-4 commits (更快) | L81 (L63→L77) / L86 (L77→L85) |
| STATUS-2026-05-11 §N append | ~5-6 commits | L79 §13 covering L47→L78 / L84 §14 covering L79→L83 / L? §15 covering L84→L86 (本节，提前到 3 commits 内 append) |

H2 check 13 仅锁 H_N 数字基线 3-site mirror（CLAUDE-NOTES canonical /
handoff / sweep header），**不锁 L-anchor**。任何 doc 里出现的 `L<N>`
都是手工维护，自动检测需要新 tier-4 check 把 max-L-in-docs 跟 ledger
row count 关联。未实现。

### 15.4 L86 后基线（与 L83 后一致）

```
H1=26 / H2=46 / H3=26 / H4 partial / H5 partial / H6=39 / H7 partial
fixtures 36/0/13；V1.5 27/27 不变；ai-native cppunit OK(84) claimed /
OK(77) workdir-verified（B2 blocks controller 7-case 升级）
ledger 86 行
```

L84-L86 无任何 harness/baseline 变化。3-site mirror 仍 H2=46 对齐。

### 15.5 仍开放（与 §13.5 / §14.6 一致）

D1/D3/D5/D6/D8/D9 + W4/W5 scope auth：全部 user-only。
B1 / B2 / B3 三类环境阻塞依旧。

## 16. 凌晨增量 II (2026-05-12 dawn-late, L87→L89)

> §15 是 L86 frozen-snapshot；本节 append §15 之后 L87→L89 同一 /goal
> session 的 3 个 commit，其中 L89 把整段 cadence-drift class 从手工
> cleanup 转成 tier-4 自动 lock。**conflict 时本节为准**。

### 16.1 一句话

L87-L89 共 3 commit：L87 STATUS §15 append；L88 handoff anchor + 7-file
memory L-anchor batch refresh；**L89 H2 check 17 = L-anchor cross-doc
cadence ≤ 3 lock（tier-4 第 6 个 self-consistency check）**，H2 baseline
46→47。Cadence cluster (L85-L89) 在此真正闭合：drift class 由 5 次
手工 fix → 1 个 CI-blocking check。

### 16.2 3 个 commit（L87-L89）

| L | commit | 摘要 |
|---|---|---|
| L87 | `1064d93` | STATUS-2026-05-11 §15 dawn append L84-L86 + cadence quantitative summary（本文档 §15）|
| L88 | `8d407da` | handoff title `(L85)`→`(L88)` + state line 17 + line 24 enumeration L85→L87 + 7-file off-repo memory L-anchor batch refresh（L42-L84→L42-L87）|
| L89 | `ca57b1b` | **H2 check 17 L-anchor cross-doc cadence ≤ 3 lock**（tier-4 第 6 个 check），canary 17A/17B 现场验证 fail-loud + restore 干净 |

### 16.3 H2 check 17 (新 tier-4 lock, post-L89)

3 个 sentinel L-anchor 抽取 + invariant `max - min ≤ 3`:

- (a) `docs/v2-coordinator-handoff-2026-05-10.md` 标题 `(L<N>)`
- (b) `docs/CLAUDE-NOTES.md` `refreshed YYYY-MM-DD / L<N>`
- (c) `docs/product/v2/STATUS-2026-05-11.md` 最新 section header
  `L<X>→L<Y>` max Y across all such ranges

Tolerance 3 的实证依据：

- 紧 (≤ 1 或 ≤ 2)：cadence pattern L74/L78/L82/L85 显示 handoff 自然
  refresh interval 是 4 commits — 强制每 commit touch 3 docs 不现实
- 松 (≥ 5)：让一个完整 append cycle 流过才 catch，等同 L81/L86 的
  手工 cleanup 模式 — 不解决问题
- = 3：当 anchor 漂移到自然 cadence 边缘时 fail，时机正好

Canary 现场跑过 + restore:

- 17A: 把 CLAUDE-NOTES anchor 改 `L85→L77`，gap=11 FAIL with diagnostic
- 17B: 把 handoff title 改 `(L88)→(L82)`，gap=4 FAIL with diagnostic

### 16.4 4-tier locking architecture (post-L89)

| Tier | 检查 | 范围 | 自动伸展 |
|---|---|---|---|
| 1 schema↔C++ | H1+H4/5/7 | apply-plan/runtime/async-task/inline-action 状态字 | C++ header 落地后 partial→full 自动促进 |
| 2 schema↔manual | H6 | 4 reader's manuals fact-block ↔ schema body | glob-discovered，新 `*.schema.md` 自动入册 |
| 3 lane-status↔artifacts | H2 9/10/11 | ledger-count, manual roster, harness path+0755 | 双向；任一漂移 fail CI |
| **4 self-consistency** | **H2 12/13/14/15/16/17** | row-shape, baseline 3-site, sweep exec-bit, sweep↔disk parity, ledger ts ISO+UTC monotonicity, **L-anchor cadence ≤ 3** | 锁住 locking 体系自身 + 跨 doc anchor 同步；L65-L89 一系列事件迭代而成 |

tier-4 累计 6 个 self-consistency lock，每个源于具体 drift 事件:
L65 row 多键 → L66 check 12 / L67 baseline 镜像漂移 → L67 check 13 /
L70 sweep 失 0755 → L70 check 14 / L76 新 harness 未入 sweep → L76
check 15 / L82 ledger ts mixed-tz string compare 误报 → L83 check 16 /
**L74/L78/L82/L85 + L81/L86 + L84/L87 + L88 cadence cluster 5+ 次手工
fix → L89 check 17**。

### 16.5 L89 后基线

```
H1=26 / H2=47 / H3=26 / H4 partial / H5 partial / H6=39 / H7 partial
fixtures 36/0/13；V1.5 27/27 不变；ai-native cppunit OK(84) claimed /
OK(77) workdir-verified（B2 blocks controller 7-case 升级）
ledger 89 行
```

3 处镜像（H2 check 13 enforce）对齐 H2=47：CLAUDE-NOTES（canonical）
+ handoff doc `Current pass-count baselines` 行 + `bin/v2-harness-sweep.sh`
header `(post-L89)`。

### 16.6 Cadence cluster 闭合

L85-L89 共 5 commit 全部 doc/test/lock 工作，**L89 是真正闭合点**：
观察到的 drift class（"我每 4 commit 又要修一遍 handoff title/anchor"）
转成 CI-blocking check。下个 /goal session 如果 cadence 又超界，
sweep 会在 commit 前 fail，不再手工 cleanup。

### 16.7 仍开放（与 §13.5 / §14.6 / §15.5 一致）

D1/D3/D5/D6/D8/D9 + W4/W5 scope auth：全部 user-only。
B1 / B2 / B3 三类环境阻塞依旧。

## 17. 凌晨增量 III (2026-05-12 dawn-late II, L90→L93)

> §16 闭合了 cadence cluster (L85-L89)；本节 append §16 之后 L90→L93
> 同一 /goal session 的 4 个 commit，其中 L93 把整套 8-canary protocol
> 从"声称 fail-loud"升级为"逐条 verified 真 catch drift"。**conflict
> 时本节为准**。

### 17.1 一句话

L90-L93 共 4 commit：L90 STATUS §16 append + check 17 first-enforce；
L91 handoff line 24 narrative collapse；L92 check 17 comment self-anchor
de-anchor；**L93 = canary 8/8 全 verified 真 catch drift + 暴露并修 2
个 protocol bug (16A `.tmp` leak / 17A hardcoded-date silent no-op)**。
H2 baseline 仍 47（无新 check，本 turn 加固既有 8 canary 的实证强度）。

### 17.2 4 个 commit（L90-L93）

| L | commit | 摘要 |
|---|---|---|
| L90 | `895b42f` | STATUS-2026-05-11 §16 dawn-late append L87-L89 + cadence cluster 闭合 + check 17 first-enforce 实证（本文档 §16）|
| L91 | `5391126` | handoff line 24 stale-enumeration collapse → 1-line reference 'see STATUS §13-§16 + ledger rows 65-89' + forward-looking rule |
| L92 | `a642b5a` | tests/v2-plan-baseline-test.sh line 387 check 17 comment self-anchor de-anchor (meta-drift class kill, 跟 L91 同模式) |
| L93 | 本次 | **canary 8/8 全 verified 真 catch drift** + 2 protocol bug fix (16A `sed -i.tmp` leak → `sed -i ''` / 17A hardcoded date silent no-op → date-agnostic regex) |

### 17.3 Canary 8/8 全 verified（L93 turn 现场）

每个 canary：mutate → run harness → FAIL with exit=1 → restore → PASS →
post-restore working-tree clean。本 turn 第一次系统化跑全 8 条，不是
"应该会 fail"声称、是 actually verified.

| canary | check | mutation | mutated result | restore result |
|---|---|---|---|---|
| 13 | H2 13 baseline mirror | CLAUDE-NOTES 46→45 | FAIL exit=1 | PASS |
| 14 | H2 14 sweep 0755 | chmod 644 sweep.sh | FAIL exit=1 | PASS |
| 15A | H2 15 sweep↔disk parity | add stub tests/v2-fake-test.sh | FAIL exit=1 | PASS |
| 15B | H2 15 (reverse) | comment harness entry in sweep | FAIL exit=1 | PASS |
| 16A | H2 16 ts ISO+UTC monotonic | bad ts string in ledger | FAIL exit=1 | PASS (after fix) |
| 16B | H2 16 (reverse) | reorder ledger rows | FAIL exit=1 | PASS |
| 17A | H2 17 L-anchor cadence | CLAUDE-NOTES L→L69 | FAIL exit=1 | PASS (after fix) |
| 17B | H2 17 (reverse) | handoff title (L90)→(L80) | FAIL exit=1 | PASS |

8/8 真 catch + 8/8 restore 干净 + 0 leak。Production gate: canary
protocol 从"protocol-level claim"升级为"row-by-row verified"。

### 17.4 暴露并修的 2 个 protocol bug

**16A**：BSD `sed -i.tmp` 留 `.agent/goals/.../ledger.jsonl.tmp` orphan，
未 cleanup → 下次 git status 噪声、潜在 `git add .` 风险。
Fix: `sed -i ''`（empty backup ext，BSD 安全 in-place），并删掉无用的
`rm -f *.tmp` cleanup 行（既然 no .tmp 产生）。

**17A**：原 mutation regex `'refreshed 2026-05-11 / L8[0-9]'` hardcode
日期，CLAUDE-NOTES 实际 anchor 行已是 `refreshed 2026-05-12`，sed 静默
no-op，跑下来 mutated 也 PASS — 假 verified。Fix: date-agnostic regex
`'/ L8[0-9])'` 只锚 L-anchor 段，跨日期稳定。

两个 bug 都是 protocol-doc 落地后从未被 actually-run 暴露 → tier-4
production gate "claimed fail-loud" 与 "verified fail-loud" 的 gap。

### 17.5 L93 后基线

```
H1=26 / H2=47 / H3=26 / H4 partial / H5 partial / H6=39 / H7 partial
fixtures 36/0/13；V1.5 27/27 不变；ai-native cppunit OK(84) claimed /
OK(77) workdir-verified（B2 blocks controller 7-case 升级）
ledger 93 行
```

3 处镜像（H2 check 13 enforce）对齐 H2=47：CLAUDE-NOTES（canonical）
+ handoff doc `Current pass-count baselines` + `bin/v2-harness-sweep.sh`
header `(post-L89)`（baseline 数字未变，header L-anchor 故意不随 cadence
移动，避免 meta-drift — 跟 L92 de-anchor 同立场）。

### 17.6 Drift class 3-layer closure (canary protocol 层)

跟 §16.6 cadence cluster 闭合同模式，本 turn 闭合 canary protocol drift
class:

| 层 | 触发 | 闭合 |
|---|---|---|
| 1 manual | 写 canary 时 BSD/GNU sed 行为差异、hardcoded date 隐患 | 手工 fix 单 canary |
| 2 anchor | L89/L92 check 17 + comment de-anchor | tier-4 lock anchor 漂移 |
| 3 **structural** | L93 8/8 全 verified + 2 bug fix → protocol 从 claimed → tested | 任何新 canary 写完必须 actually-run mutate+restore 一遍 |

下次新加 canary，protocol 必须 verified-by-run 才能 merge — 不再依赖
"看起来对"。

### 17.7 仍开放（与 §13.5 / §14.6 / §15.5 / §16.7 一致）

D1/D3/D5/D6/D8/D9 + W4/W5 scope auth：全部 user-only。
B1 / B2 / B3 三类环境阻塞依旧。
async-task schema state enum 6 项 vs fixtures 仅覆盖 3 项 — 真 invariant
gap 但 add fixtures = W5 spec auth user-only，本 turn 不动。
